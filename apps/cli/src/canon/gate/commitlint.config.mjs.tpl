// commitlint.config.mjs — scaffolded by `cospec init --gate`.
// config-conventional's default type-enum IS the 11 conventional-commit types that cospec
// exposes as change schemas (`cospec new <type>`), so no explicit type-enum is needed here.
// Add a `rules.scope-enum` of your own to constrain scopes.
export default {
  extends: ['@commitlint/config-conventional'],
}
