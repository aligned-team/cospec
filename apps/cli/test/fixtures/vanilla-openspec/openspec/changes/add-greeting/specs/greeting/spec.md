# greeting

## ADDED Requirements

### Requirement: Personalized greeting

The system SHALL return a greeting that includes the provided name.

#### Scenario: Greet a named user

- **WHEN** a caller requests a greeting for the name "Ada"
- **THEN** the response contains "Hello, Ada"
