[tasks.build]
description = "Compile copsx to a single executable at dist/copsx"
run = "bun build --compile --minify --bytecode src/index.ts --outfile dist/copsx"

[tasks.dev]
description = "Run copsx from source"
run = "bun run src/index.ts"
