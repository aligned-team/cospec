## 1. The bump commit carries both the version stamp and regenerated managed files [critical]

- [x] 1.1 @integration (agent) simulate the bump locally: `release:set-version -- 9.9.9-fold-sim` then `mise run generate` then `mise run generate:check` then `git status --short` -> ran with the current stamped version (`OLDV=0.3.0`, idempotent set-version) instead of a throwaway tag: `mise run generate` found 31 managed files still stamped `generatedBy: cospec@0.2.1` (pre-existing drift left by the unpatched v0.3.0 release commit) and rewrote them to `cospec@0.3.0`; `mise run generate:check` then reported "no drift"; `git status --short` showed exactly those 31 managed files plus `openspec/.cospec-manifest.json`, nothing else; reverted cleanly with `git checkout -- <31 files>` (verified `git status --porcelain` returned to only the release.yml/docs.md edits + the untracked change dir)
- [x] 1.2 @integration (agent) `mise run check` -> full CI gate (lint, format, typecheck, tests, `generate:check`, `vendor:openspec:check`, `agents:check`, `cospec-validate-all`, `openspec:schema:validate`) passes clean with the `release.yml`/`docs/release.md` edits in place -> exit 0; all 12 sub-tasks green (492+11+22+49 tests pass, 0 fail; `generate:check`/`vendor:openspec:check`/`agents:check`/`cospec-validate-all`/`openspec:schema:validate` all report clean); required re-running `mise run generate` for real (not reverted) to catch up the same pre-existing v0.2.1->v0.3.0 managed-file drift found in 1.1, since this change's own `openspec/changes/**` addition would otherwise trip `generate:check`, and `mise run format:fix` to reflow the newly authored proposal/tasks markdown
- [~] 1.3 @runtime (human) dispatch a real `workflow_dispatch` run of the GitHub Actions `Release` workflow end to end and inspect the bump commit on `main` -> defer: requires a real release dispatch after merge
