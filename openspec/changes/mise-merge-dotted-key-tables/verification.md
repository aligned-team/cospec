## 1. Dotted-only [tools] table merges correctly [critical]

- [x] 1.1 @regression (agent) unit-test `mergeMiseToml` against a `mise.toml` fixture with `tools.node = "20"` and no `[tools]` header, missing gate tool pins -> before fix: status `'unparseable'` with misleading "not valid TOML" message, no keys added (red); after fix: status `'merged'`/`'conflict'`, every missing gate key present in dotted form, content re-parses as valid TOML (green) -> test #13 in mise-merge.test.ts: status is not 'unparseable', added contains tools.hk/tools.bun, content contains `tools.hk = "1"` with no re-opened `[tools]` header, re-parses and `tools.node`/`tools.hk` both present. PASS.
- [x] 1.2 @unit (agent) run the full existing `mise-merge.test.ts` suite -> all prior literal-header cases still pass unchanged -> `bun test` on the suite: 20 pass / 0 fail / 108 expect() calls (15 pre-existing cases unchanged + 5 new dotted-key cases, including the context-relative regression in 3.2). PASS.

## 2. Mixed dotted-key and literal-header tables in one file

- [x] 2.1 @unit (agent) unit-test a fixture with one table defined via dotted keys and another via a literal `[header]`, both missing gate keys -> both tables receive their missing keys, output re-parses, neither insertion regresses the other -> test #14: dotted `tools.node` preserved and `tools.hk` inserted in dotted form; literal `[hooks]` receives `postinstall`; content re-parses with both present. PASS.

## 3. Dotted [tasks."cospec:*"] table

- [x] 3.1 @unit (agent) unit-test a fixture where a `[tasks."cospec:validate"]`-equivalent table is defined via dotted keys (e.g. `tasks."cospec:validate".description = "..."`) missing the gate's `run` key -> missing key inserted in dotted form, table not duplicated or reopened with a `[header]`, output re-parses -> test #15: `tasks."cospec:validate".description` preserved verbatim, `tasks."cospec:validate".run` inserted in dotted form, no literal `[tasks."cospec:validate"]` header emitted, content re-parses with both keys present. PASS.
- [x] 3.2 @regression (agent) unit-test a fixture where the target subkey sits under an open `[tasks]` header rather than at root (e.g. `[tasks]\n"cospec:validate".description = "..."`), missing the gate's `run` key -> before fix: the locator matched inside the open `[tasks]` context but the emitted line was root-relative (`tasks."cospec:validate".run`), which TOML resolves as `tasks.tasks."cospec:validate".run` under the still-open header, so the intended key never landed and the merge falsely reported `'unparseable'` (red, empirically confirmed); after fix: the inserted line is context-relative (`"cospec:validate".run`), lands at the intended path, content re-parses (green) -> test #15b in mise-merge.test.ts: status is not 'unparseable', content re-parses, `tasks['cospec:validate'].description` and `.run` both present, and the content never contains the root-relative `tasks."cospec:validate".run` form. PASS.

## 4. Locator-miss fallback is honest

- [x] 4.1 @unit (agent) unit-test a fixture engineered so the dotted-key locator cannot place a target table -> reported status/message never contains "not valid TOML"; it names the actual reason (dotted-key table / insertion point not found), and any other independently-locatable table in the same file still merges -> test #16: `tools = { node = "20" }` (inline table) with a companion literal `[hooks]` table -> status `'conflict'` (never `'unparseable'`), snippet contains no "not valid TOML" and matches /dotted-key|insertion point/, a conflict entry's `existing` field reads "(dotted-key table — insertion point not found)", and `[hooks]`/`postinstall` still merge into content. PASS.

## 5. Docs and init receipts stay accurate

- [x] 5.1 @manual (agent) diff `apps/docs/guide/installation.md`'s mise.toml merge description against the fixed statuses/messages -> updated only if the user-facing text changed, otherwise confirmed already accurate -> installation.md's mise.toml section only says init "additively merges the gate's tasks and tool pins ... leaving any conflicting values as-is with a paste-ready snippet" — no status enum or exact message text is quoted, so it remains accurate for both the dotted-key merge path and the locator-miss fallback (still surfaced as a scoped, paste-ready snippet). No edit needed. Also grepped `apps/cli/test/unit/init/init.test.ts` and `apps/cli/test/integration/init.test.ts` for "not valid TOML"/"unparseable"/"dotted" — no matches, so no stale wording to update there either.
