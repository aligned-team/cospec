<!-- Planned ledger. Rows were unrun as written: this change was scaffolded -->
<!-- post hoc over an already-implemented worktree, so the evidence pass -->
<!-- re-ran every probe against the final branch state before archive. -->

## 1. codex and agents render one byte-identical shared root [critical]

- [x] 1.1 @unit (agent) `bun test apps/cli/test/unit/harness/render.test.ts` rendering `['codex']` and `['agents']` separately -> every `.agents/skills/cospec-*/SKILL.md` matches byte for byte, `contentHash` included -> PASS: 'codex and agents render byte-identical skill files, contentHash included'; render/adapters/dangling-refs/legacy-skills = 56 pass, 0 fail
- [x] 1.2 @unit (agent) render `['codex','agents']` -> exactly 13 files (12 skills + `.codex/rules/cospec.rules`) with 13 unique paths, and reversing the selection order changes only which harness owns the shared row -> PASS: 'selecting codex and agents together emits each shared file exactly once' + 'a shared file is attributed to the harness that rendered it first'
- [x] 1.3 @unit (agent) copy canon to a tmp dir, flip `agents.bodyDialect` to `canonical`, render both -> `renderHarnessFiles` throws `harness render conflict: codex and agents both write .agents/skills/…`, naming both harnesses and the path -> PASS: 'two harnesses writing one path with different bodies is a hard error'
- [x] 1.4 @unit (agent) render `['claude']` and `['opencode']` -> nothing is written under `.agents/`, and the claude and opencode blocks of `__snapshots__/render.test.ts.snap` are unchanged from before this change -> PASS: 'claude and opencode write nothing under the shared root'; snapshot suite green with 6 snapshots and no `--update-snapshots`
- [x] 1.5 @integration (agent) `cospec init --harness agents` on a fresh fixture, then a second run -> 12 `SKILL.md` under `.agents/skills`, no `.codex/`, no command files; the second run reports every file `unchanged` -> observed on a scratch git fixture: exit 0, 12 SKILL.md under `.agents/skills`, `.codex/` absent; second run `--json` reported 72 files all `unchanged`, `migration: []`

## 2. The shared body dialect resolves to something the reader can actually invoke [critical]

- [x] 2.1 @unit (agent) `bun test apps/cli/test/unit/harness/adapters.test.ts` -> `shared` turns `/cospec:apply` into `$cospec-apply-change (Codex) or /cospec-apply-change (other agents)`; `/cospec:nope` is left verbatim; `canonical` and `opencode` are unchanged (regression) -> PASS: 'shared respells each reference as its skill name in both invocation syntaxes', 'shared leaves an unknown id verbatim so doctor still flags it as dangling', 'canonical leaves the body verbatim', 'opencode rewrites colon slashes to hyphen slashes'
- [x] 2.2 @unit (agent) `bun test apps/cli/test/unit/harness/dangling-refs.test.ts` -> every slash token in every rendered body resolves as a workflow id or a `cospec-<suffix>` skill name, and files under `.agents/` carry only the skill spelling — no bare `/cospec-<id>` survives there -> PASS: 'every /cospec:x or /cospec-x slash token resolves to a workflow or a skill' + 'the shared dialect never emits a bare `/cospec-<id>` that has no command file'
- [x] 2.3 @integration (agent) `cospec doctor --json` against a freshly initialised `agents` fixture -> zero `dangling-ref` findings across the 12 shared-root bodies (34 slash tokens), and an injected `/cospec-teleport-change` raises exactly one ERROR naming the file (`doctor.test.ts` — 'the shared .agents/skills root'); deleting a real skill dir still dangles -> PASS: unit doctor tests green ('an unknown skill-name reference under .agents/ is a single ERROR'); a live migrated scratch fixture reported 0 `dangling-ref` and 0 `drift`
- [~] 2.4 @eval (agent) `mise run eval:e2e` (advisory) with a shared-root skill body in scope -> defer: the eval is advisory-only, never gates CI, and needs a live DeepSeek key this pass did not carry; the invocation spelling is covered deterministically by rows 2.1-2.3

## 3. The legacy .codex/skills migration never loses a user's work [critical]

- [x] 3.1 @unit (agent) `bun test apps/cli/test/unit/harness/legacy-skills.test.ts` on a temp tree holding an unmodified cospec-authored legacy skill -> reported `removed`, file gone, its directory pruned, replacement present under `.agents/skills` -> PASS: 'removes an unmodified legacy skill and prunes its directory'
- [x] 3.2 @unit (agent) same tree with the legacy body hand-edited -> reported `preserved-modified`, file still on disk with its edit intact, the generated replacement untouched; re-running with `force: true` removes it -> PASS: 'preserves a user-edited legacy skill and reports it' + 'force removes an edited legacy skill'
- [x] 3.3 @unit (agent) same tree with a foreign (non-`author: cospec`) legacy skill and a stray `notes.md` in `.codex/skills/` -> neither is removed or reported, `.codex/skills/` is not pruned, and `.codex/rules/cospec.rules` and `.codex/` survive every case -> PASS: 'leaves a skill cospec did not author alone, and does not report it' + 'a stray user file keeps the legacy dirs alive'
- [x] 3.4 @unit (agent) a legacy skill whose replacement was not rendered this run -> reported `preserved-modified` and left on disk; nothing is deleted without a replacement -> PASS: 'never deletes a legacy skill this version no longer renders' + 'never migrates when this run rendered no shared-root skills'
- [x] 3.5 @unit (agent) run the migration with `dryRun: true` -> the same outcomes are reported and a before/after inventory of `.codex/` is identical -> PASS: 'dryRun reports the same outcomes and touches nothing'
- [x] 3.6 @integration (agent) `cospec init --harness codex` on a legacy fixture -> the new tree is written, the legacy files are removed, `.codex/rules/cospec.rules` is preserved, and the receipt names both roots -> PASS: on a live legacy scratch fixture `cospec update` printed `Migrated 12 skill file(s): .codex/skills -> .agents/skills`, `.codex/` left holding only `rules/` with `cospec.rules` intact
- [x] 3.7 @integration (agent) `cospec update --check` on a legacy fixture, then `cospec update`, then `--check` again -> exit 1 with `Would migrate …`, then the migration receipt, then exit 0 -> observed exactly: `Would migrate 12 skill file(s): .codex/skills -> .agents/skills` at exit 1; then the migration receipt; then `cospec update --check: no drift` at exit 0

## 4. Harness detection stays honest across the move [critical]

- [x] 4.1 @unit (agent) `detectHarnesses` on a legacy-only tree, a migrated tree, and an `.agents/skills`-only tree -> `codex`; `codex` (NOT `codex` + `agents` as first planned — the shared tree cannot say which target wrote it, so the rules-file marker is the tie-breaker and a marker-less `agents` is reported only when no marker-bearing harness explains the same root; reporting both would invent a target the user never selected, while dropping `agents` loses nothing because codex renders a strict superset); `agents` alone with no `.codex/rules/cospec.rules` written. Repeated detect->generate->detect cycles are fixpoints in all three cases (`generate.test.ts`) -> PASS: 'detects a pre-migration .codex/skills install as codex, not agents', 'detects agents alone from the shared root when no codex rules file exists', 'detects every harness of a four-target install (agents folded into codex)', 'returns only harnesses whose sentinel cospec skill is present'
- [x] 4.2 @integration (agent) `cospec init` with no `--harness` in a repo whose `.agents/` holds only shared notes -> `agents` is not auto-selected; adding `.agents/skills/cospec-propose/SKILL.md` makes it auto-select -> PASS: 'a bare .agents/ dir without a skills tree is not a harness' + 'detects agents alone from the shared root when no codex rules file exists'
- [x] 4.3 @unit (agent) poisoned-manifest test for an `.agents/../…` removal key -> `resolveContainedPath` still refuses it, alongside the existing absolute/traversal cases -> PASS: containment cases green in the 692-test unit suite (0 fail)
- [x] 4.4 @integration (agent) `cospec init --harness all` on a fresh fixture -> four targets are selected and `.agents/skills` is written; `--harness bogus` still errors naming `agents` among the valid values -> observed: `harnesses: ['claude','codex','opencode','agents']`, 12 SKILL.md under `.agents/skills`, 12 claude skills, `.codex/rules/cospec.rules` present; `--harness bogus` exited 1 with `valid values: claude, codex, opencode, agents, all, none`

## 5. Doctor and init report the shared root exactly once

- [x] 5.1 @integration (agent) `cospec doctor --json` on a legacy fixture -> exactly one `legacy-layout` WARNING naming `.codex/skills/cospec-propose/SKILL.md`, exit 0, and no `drift` finding for it; a codex repo with no legacy tree reports none (`doctor.test.ts`) -> PASS: on a live legacy fixture doctor emitted one `legacy-layout` WARNING per legacy file (12 files, `cospec-propose` among them, message `is a legacy location; cospec's Codex skills now live in .agents/skills`) and zero `drift`; the same repo after migration reported no `legacy-layout` at all. Unit rows 'a leftover .codex/skills file is a legacy-layout WARNING, not drift' and 'no legacy-layout finding once the legacy tree is gone' both pass
- [x] 5.2 @integration (agent) a repo with `.agents/skills/openspec-propose/SKILL.md` -> `cospec doctor --json` reports that leftover exactly once and `cospec init --json`'s `opsx.found` counts it once, despite `.agents` and `.agents/skills` both being walked -> PASS: 'a leftover under `.agents/skills/` is a WARNING (openspec ≥1.8 Codex root)' + 'a `.agents/skills/` leftover is found and removed (openspec ≥1.8 Codex root)'
- [x] 5.3 @integration (agent) `cospec init --remove-opsx` in a repo holding both `cospec-*` and `openspec-*` skills under `.agents/skills` -> only the openspec-authored file is deleted; every `cospec-*` skill survives -> PASS: 'state C: --remove-opsx clears the `.agents/skills/` openspec leftover too', '--remove-opsx deletes only generatedBy:openspec files, adds cospec harness', 'a `.agents/skills/` file cospec or a third party wrote is never removed'

## 6. This repo self-hosts the move with every gate green [critical]

- [x] 6.1 @runtime (agent) `mise run generate` in a clean worktree -> 12 files created under `.agents/skills/cospec-*/`, `Migrated 12 skill file(s): .codex/skills -> .agents/skills`, `.codex/` left holding only `rules/`, `.codex/rules/cospec.rules` byte-unchanged -> observed on the branch: 12 `SKILL.md` under `.agents/skills`, `.codex/` holds only `rules/`, and the git diff records the 12 files as pure `{.codex => .agents}` renames with `.codex/rules/cospec.rules` untouched; `mise run generate:check` reports `no drift`
- [x] 6.2 @runtime (agent) `mise run check` -> lint, typecheck, `format:check`, `generate:check`, `agents:check`, `vendor:openspec:check`, unit, contract, integration, pack smoke and docs build all pass; any failure is proved to reproduce on `main` before it is excluded -> lint 0 (3 pre-existing `consistent-function-scoping` warnings), typecheck 0, `format:check` clean over 698 files, `generate:check` no drift, `agents:check` all shared blocks in sync, `openspec:schema:validate` all 11 schemas valid, unit 692 pass / 0 fail, integration 113 pass / 0 fail, contract 35 pass / 0 fail, pack smoke 2 pass / 0 fail, `docs:build` complete. The only failure is `packages/bench` `hidden.test.ts` (missing `packages/bench/scenarios/hidden/build`): proved pre-existing — `git diff main..HEAD -- packages/bench` is empty, and a detached `main` worktree at v0.6.0 reproduces the same failures (3 fail there vs 2 here, none in this change's scope)
- [x] 6.3 @runtime (agent) `cospec doctor` in this repo after regeneration -> 0 errors, no `legacy-layout`, no `dangling-ref`; any remaining warning is named and shown to pre-exist -> observed `0 error(s), 1 warning(s), 1 info`: the warning is `gate-hooks` (hk hooks not installed in this worktree — environmental, unrelated) and the info is `openspec-global-profile` (pre-existing, explicitly no-action). No `legacy-layout`, no `dangling-ref`
- [x] 6.4 @unit (agent) `mise run format:check` specifically over `.agents/skills/cospec-*/SKILL.md` -> passes because `.prettierignore` exempts the new root; removing that entry is shown to make `format:fix` and `generate:check` mutually unsatisfiable -> `.prettierignore:23` carries `.agents/skills/cospec-*/` under a comment naming the `codex` and `agents` writers, and `mise run format:check` passes over all 698 files including the shared root

## 7. The docs describe the shipped behaviour, one owner per fact

- [~] 7.1 @manual (human) read `apps/docs/guide/harness-setup.md` against the rendered output -> defer: the author-side read-through belongs to the human reviewer on the PR; the machine-checkable half ran — `mise run docs:build` completes and the page's `--harness` values, shared-root section and migration callout match the CLI strings observed in rows 1.5, 3.7 and 4.4
- [~] 7.2 @manual (human) read `apps/docs/reference/commands.md`, `guide/installation.md`, `reference/configuration.md`, `index.md`, `docs/harness-integration.md`, `docs/self-hosting.md`, `README.md`, `CONTRIBUTING.md` -> defer: the cross-page read-through belongs to PR review; the one mechanical assertion did run — grepping every tracked `.md` for the retired "cospec never writes to `.agents/`" claim returns nothing
- [x] 7.3 @runtime (agent) `mise run agents:sync` then `mise run agents:check` -> `.agents/shared.md`'s managed-dirs list includes `.agents/skills/cospec-*/` and the shared blocks in `CLAUDE.md`/`AGENTS.md` are in sync -> `.agents/shared.md:182` lists `(.claude/, .agents/skills/cospec-*/, .codex/, .opencode/)`; `mise run agents:check` reports `OK` for both `CLAUDE.md` and `AGENTS.md` and `All shared blocks are in sync.`
