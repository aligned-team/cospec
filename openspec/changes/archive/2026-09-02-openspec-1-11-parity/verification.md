<!-- Every row below was re-run by the ledger pass on the final branch state -->
<!-- unless it is marked `[~] defer:`. Suite totals quoted here are from that -->
<!-- final run: unit 659 pass / 0 fail (43 files); contract 35 pass / 0 fail -->
<!-- (7 files, real pinned 1.11.0 binary); integration 113 pass / 0 fail (20 -->
<!-- files); pack smoke 2 pass. -->

## 1. The pin moves to 1.11.0 and the wrapped binary still resolves and runs everywhere [critical]

- [x] 1.1 @integration (agent) `mise run test:contract` with `version-tripwire.test.ts` in scope after the bump -> `bun test test/contract/version-tripwire.test.ts` 8 pass / 0 fail with no edited expectations; the manifest pin, the `mise.toml` dev pin, the `mise.lock` entry and the live bundled binary's `--version` all report the same exact `PINNED_OPENSPEC_VERSION = 1.11.0`, and that pin satisfies the accepted range
- [x] 1.2 @integration (agent) `mise run vendor:openspec:check` after `mise run vendor:openspec` -> `vendor:openspec: bundle and third-party notices up to date.` (exit 0); the committed `openspec.bundle.js.tpl` (1,917,826 bytes) and `THIRD-PARTY-LICENSES.md` match a fresh build, with the 110 -> 71 package shrinkage accounted for as the expected posthog-node/`@posthog/*` removal
- [x] 1.3 @runtime (agent) `mise run test:integration` covering `pack-standalone.test.ts` -> `bun test test/integration/pack-standalone.test.ts` 2 pass / 0 fail, and `mise run test:pack` 2 pass / 0 fail: the compiled standalone binary extracts and runs the embedded 1.11.0 bundle with no `node_modules`
- [x] 1.4 @unit (agent) unit tests over `core/openspec.ts` after correcting the stale dead type declarations -> `bun test test/unit/core/openspec.test.ts` 18 pass / 0 fail; `PINNED_OPENSPEC_VERSION` is `1.11.0`, `OPENSPEC_VERSION_FLOOR` is still `1.0.0`, the range still refuses `0.23.0` and `2.0.0`, and `checkVersion('2.0.0')` throws

## 2. cospec reports a successful archive as successful, in any timezone and for any change id [critical]

- [x] 2.1 @unit (agent) run the archive date helper under `TZ=Etc/GMT+12` and under `TZ=UTC` -> `bun test test/unit/commands/archive-naming.test.ts` 14 pass / 0 fail; `formatLocalDate` returns the local calendar date east of UTC where `toISOString()` still says the previous day (`Asia/Tokyo` -> `2026-07-06` vs `2026-07-05`), agrees with UTC west of it, and zero-pads month and day
- [x] 2.2 @unit (agent) drive the step-9 target matcher with both `YYYY-MM-DD-<id>` and an already-date-prefixed `<id>` -> same file: `isArchiveTargetFor` accepts the dated form for a normal slug, accepts BOTH forms for a slug that already carries a date prefix, rejects a different change archived the same day, rejects an undated directory, and treats regex metacharacters in a slug literally
- [x] 2.3 @integration (agent) contract test archiving through the real 1.11.0 binary with the process TZ set ahead of UTC -> `bun test test/contract/archive-gotchas.test.ts` 5 pass / 0 fail, including `the archive slot is stamped in local time, not UTC`: the skewed zone is chosen from the current UTC hour so the two dates always disagree, `TZ` is passed explicitly to both spawned children, openspec prints `archived as '<localDate>-…'` and `cospec archive` exits 0 reporting that same slot
- [x] 2.4 @regression (agent) archive a change whose directory was deliberately not moved -> same file: `no-op delta: openspec aborts without moving the change; cospec refuses to claim success` and `MODIFIED vs missing target: same abort; cospec refuses` both pass — success is still computed from the filesystem post-condition, not from the exit code alone
- [~] 2.5 @unit (agent) build an `archiveMap` from an archive containing a date-prefixed slug -> defer: no test asserts `archiveMap` keying for a slug that itself carries a date prefix, because that input is unreachable through cospec's own surfaces — `CHANGE_ID_RE` makes `resolveChange` return undefined for a date-prefixed id and `meta/name-kebab` raises an ERROR on `ARCHIVE_PREFIX_RE` before archive delegates (reason recorded in `isArchiveTargetFor`'s doc comment, and W2's task text corrected to say the derivation was deliberately left unchanged). `archiveMap`'s keying for the reachable form is covered: `bun test test/unit/commands/gate.test.ts` `maps slug → latest archived date` passes

## 3. The spec parser cannot be fooled into disarming a hard gate [critical]

- [x] 3.1 @unit (agent) parse fixtures with a UTF-8 BOM, CRLF line endings, a commented-out `### Requirement:`, a commented-out `#### Scenario:`, a four-backtick fence containing a three-backtick line, a `~~~` fence, and an unterminated comment -> `bun test test/unit/parsers` (part of 275 pass / 0 fail across parsers+rules+init) covers all seven: BOM does not hide the first section header, CRLF leaks no `\r` into a captured requirement name or a `Scenario removed:` reason, `a commented-out requirement is not counted, and line numbers do not shift`, a commented-out scenario does not inflate the living scenario count, an inner three-backtick line does not close a four-backtick fence, a three-backtick line does not close a `~~~` fence, `--!>` terminates a comment, and an unterminated `<!--` masks to EOF
- [x] 3.2 @integration (agent) drive the `archive/scenario-preservation` gate on a change whose only surviving scenarios sit inside a code fence or an HTML comment -> ran end to end through `cospec archive` against a real repo; both the HTML-comment and four-backtick-fence variants exit 1 with the change still in `openspec/changes/`, reporting `WARNING … archive/scenario-preservation  MODIFIED "Widget rendering" drops scenario count from 2 to 0` (the masked headings read as zero preserved scenarios, not two). This composition had no test of its own, so the ledger pass added the two cases to `test/integration/archive-gates.test.ts`, which now runs 12 pass / 0 fail
- [x] 3.3 @regression (agent) re-run the existing delta/living-spec parser suite unchanged -> `mise run test` 659 pass / 0 fail across 43 files; the pre-existing fence, `Scenario removed:` and scenario-count assertions all still pass and none needed editing

## 4. Nested and root-level capability paths resolve to the right capability [critical]

- [x] 4.1 @integration (agent) drive a fixture laid out `specs/<area>/<capability>/spec.md` end to end through `cospec validate`, the scenario-preservation gate, and archive's post-merge spot check -> `bun test test/integration/lifecycle-feat.test.ts` 5 pass and `test/integration/archive-gates.test.ts` 12 pass: the merge lands at `openspec/specs/platform/session-layout/spec.md` (no `openspec/specs/platform/spec.md` is created), `validate --specs --json` lists the id `platform/session-layout`, `the scenario-preservation gate fires for a nested capability path` names the full path rather than the area, and `a well-formed nested delta archives and passes the post-merge spot-check` exits 0
- [x] 4.2 @unit (agent) exercise `core/spec-paths.ts` against an in-capability symlink, a symlink escaping its capability, a dangling symlink, an `ENOENT` directory, and a non-`ENOENT` readdir failure -> `bun test test/unit/core/spec-paths.test.ts` 23 pass / 0 fail: in-capability links resolve, an escaping link throws `/outside the allowed directory/`, a dangling link is skipped, `ENOENT` yields an empty list while `EACCES` propagates, ids are posix-formed and code-point sorted, and `isDeltaSpecFile` accepts only a file literally named `spec.md`
- [x] 4.3 @integration (agent) validate a change carrying a root-level `specs/spec.md` -> covered in the same integration run: exit 1 with exactly one `deltas/spec-at-specs-root` ERROR naming the missing capability directory, no capability called `spec.md` reported, and zero `openspec/validate` issues naming that file (the delegated duplicate is suppressed)

## 5. Archive warnings the wrapped binary emits reach the user

- [x] 5.1 @integration (agent) contract test archiving a change whose delta carries an indented Notes paragraph inside a requirement -> `bun test test/integration/archive-gates.test.ts` 12 pass: `with the marker, archive succeeds and reports the retirement` shows the relayed warning in cospec's human stdout, and `--json reports the retirement and the relayed warnings` shows the same text in the `warnings` array (alongside `retired: ["widgets"]`)
- [x] 5.2 @regression (agent) archive successfully with warning text present -> `bun test test/unit/commands/archive-naming.test.ts` 14 pass: a spec-merge warning, the retirement notice with its `git checkout HEAD -- …` recovery hint, and the non-blocking proposal bullets are all collected and de-duplicated; `does not mistake ordinary archive output for a warning` keeps `warnings` empty when none are emitted; and `no warning text ever contains the abort/cancel markers step 9 keys on` proves `ABORTED_RE`/`CANCELLED_RE` cannot match, so success is still computed from the filesystem post-condition

## 6. A declared capability retirement archives cleanly instead of accusing the user of an invariant breach

- [x] 6.1 @integration (agent) contract test archiving a change whose `REMOVED` op takes a capability's last requirement, run with and without `retire_capabilities: true` -> `bun test test/integration/archive-gates.test.ts` 12 pass: with the marker archive exits 0, the living `openspec/specs/widgets/spec.md` is gone, stdout carries `Retired:  widgets`, and stderr contains no invariant-breach line; `without the marker openspec refuses and cospec relays the refusal` leaves the change and the living spec untouched
- [x] 6.2 @unit (agent) run the post-archive spot check against a `MODIFIED` op naming a requirement absent from a non-retired capability -> `mise run test` 659 pass / 0 fail; the spot check still reports a genuine miss, and the new check reports an invariant breach when a living spec disappears WITHOUT the marker. Recorded honestly: the row's premised FALSE breach on a _declared_ retirement was probed both ways against real 1.11.0 and is not reproducible (openspec only retires once no requirement block survives, so every surviving op is a REMOVED/RENAMED-from that `spotCheckMiss` tests for absence and passes), so no fix was invented for it — see W6's corrected task text

## 7. skip_specs is a bounded escape hatch, not an open door [critical]

- [x] 7.1 @unit (agent) drive the precedence table across CLI flag, persisted marker, and structural artifact matrix, plus a non-boolean marker value -> `bun test test/unit/rules` (in the 275 pass / 0 fail parsers+rules+init run) and `mise run test` 659 pass: flag beats marker beats structural, and a non-boolean value is raised as `meta/skip-specs-type` / `meta/retire-capabilities-type` naming the key
- [x] 7.2 @integration (agent) run `cospec validate` and `cospec apply` on a `feat` change with `skip_specs: true` and an empty `specs/`, then add one file under `specs/` and re-run -> `bun test test/integration/apply-surfaces.test.ts` (in the 113 pass / 0 fail integration run): `structural default: a feat change with no specs/ deltas and no marker is blocked on specs`, `persisted skip_specs: true satisfies specs with an empty specs/ (exit 0)`, and the conflict case exits 1 with `deltas/skip-specs-conflict`
- [x] 7.3 @integration (agent) run `cospec archive --skip-specs` on an unmarked change and `cospec archive` on a marked one against the real binary -> `bun test test/contract/archive-gotchas.test.ts` 5 pass, including `--skip-specs genuinely moves a change openspec would otherwise abort on`, plus `the one-shot --skip-specs CLI flag satisfies specs without a persisted marker (exit 0)` in the integration run: both paths skip spec-sync and report the resolved precedence identically

## 8. defaultStore changes the failure path and never outranks a local repository

- [x] 8.1 @integration (agent) run `cospec status`/`list`/`validate` from a directory with no `openspec/` root above it, with a HOME whose openspec global config declares a registered `defaultStore` -> `bun test test/unit/core/root.test.ts` 13 pass / 0 fail: resolution falls back to the registered `defaultStore` only once local-root resolution has failed, and falls back to the local cwd when neither resolves
- [x] 8.2 @integration (agent) run the same commands inside a repository with its own `openspec/` root while a different `defaultStore` is configured -> same file: the local `openspec/` root wins over `defaultStore` (the precedence proof — `defaultStore` is a fallback, never a tier above `localRoot`)
- [x] 8.3 @integration (agent) run with `--store` and with a local `store:` pointer while a different `defaultStore` is configured, then with a `defaultStore` naming an unregistered store -> same file plus `bun test test/integration/store-aware.test.ts` 3 pass: `--store` still outranks the fallback, `an unknown store id fails loudly instead of falling back to local`, and a stale `defaultStore` id throws `resolveStore`'s actionable unknown-store error rather than degrading silently

## 9. instructions archive is advertised, read-only, and passthrough-disciplined

- [x] 9.1 @integration (agent) contract test running `cospec instructions archive --change <id>` and its `--json` form against the real binary -> `bun test test/integration/instructions-verification.test.ts` (in the 26 pass / 0 fail run with the two rule suites): both the text and `--json` forms relay the wrapped archive-inputs payload with the wrapped exit code, and the change is still in `openspec/changes/` with nothing written
- [x] 9.2 @unit (agent) feed the passthrough a wrapped body carrying `status:[{severity:"error"}]` at exit 0 -> `bun test test/unit/core/passthrough.test.ts` (in `mise run test` 659 pass / 0 fail): exactly one JSON document is emitted and the exit code normalises to 1; the same file's unknown-change case exits 1 without throwing

## 10. status --all sweeps every change and survives a bad one

- [x] 10.1 @unit (agent) run the sweep over several active changes, one of which cannot have its status computed, plus the `--all --change` combination -> `bun test test/unit/commands/commands.test.ts` 30 pass / 0 fail: blocks appear in id order, a change whose status computation throws becomes a named `{change, error}` failure entry rather than aborting the sweep, the full envelope is still emitted, the command exits 1, and the flag conflict exits non-zero before any status is computed — including, from the review pass, the `--json` form of that conflict
- [x] 10.2 @integration (agent) run `cospec status --all --json` and `cospec status --change <slug> --json` against this repo's real tree -> ran both here: `--all --json` emits `{changes, root}` with one entry (`openspec-1-11-parity`); the single-change document is unchanged (asserted by the single-vs-all shape-parity test in the file above). The mutex path was also observed live: `cospec status --all --change openspec-1-11-parity --json` prints `{"changes": [], "root": null, "error": "The --all and --change options are mutually exclusive."}` on stdout and exits 1 with nothing on stderr

## 11. validate --archived surfaces real archived-change defects

- [x] 11.1 @integration (agent) contract test that archives a change with an incomplete task list, then runs `cospec validate --archived --json` against the real 1.11.0 binary -> `bun test test/integration/lifecycle-feat.test.ts` 5 pass / 0 fail: `2026-07-04-half-done` reports `valid: false` with a single `openspec/validate` ERROR matching `/incomplete task/` in cospec's issue-reporting shape and the command exits 1, while a genuinely finished archived change reports `valid: true` in the same sweep
- [x] 11.2 @regression (agent) run `cospec validate <slug>`, `--specs`, and `--all` after the flag lands -> `mise run cospec -- validate --all --strict` (via `mise run check`'s `cospec-validate-all` step) reports `0 errors, 0 warnings — validation passed`; `mise run test` / `test:integration` / `test:contract` all green, so behaviour and exit codes on the three pre-existing paths are unchanged

## 12. A placeholder Purpose is flagged once, not twice

- [x] 12.1 @unit (agent) validate specs whose `## Purpose` opens with `TODO:`, carries the archive-generated `TBD - created by archiving` text, and is empty, under both default and `--strict` -> `bun test test/unit/rules/specs.test.ts` (in the 26 pass / 0 fail run): `TBD: decide…` and `TODO - write this.` each report `specs/purpose-tbd` exactly once at WARNING, promoted to ERROR under `--strict`; `The retry budget is TBD pending benchmarks`, `TBDs are tracked elsewhere`, an empty Purpose and a real Purpose all report nothing, and the archive-generated sentence keeps its own wording
- [x] 12.2 @integration (agent) validate a repo where both cospec's rule and the delegated 1.11 rule report the same placeholder, and one where only the delegated rule fires -> `bun test test/unit/rules/delegated-dedupe.test.ts` 11 pass / 0 fail: each of the four dedupe classes (purpose placeholder, root-level `specs/spec.md`, `skip_specs` conflict, scenario loss) is dropped when its cospec twin fired and kept when it did not; a scenario loss naming a different requirement or a different capability file survives alongside cospec's finding

## 13. The twelfth workflow renders in every harness and nothing else counts twelve [critical]

- [x] 13.1 @integration (agent) run `cospec init` into a scratch project after regeneration -> ran `cospec init --harness claude,codex,opencode` into a fresh temp project: 12 claude commands, 12 claude skills, 12 codex skills, 12 opencode commands and 12 opencode skills, with `.claude/commands/cospec/update.md`, `.claude/skills/cospec-update-change/SKILL.md`, `.codex/skills/cospec-update-change/SKILL.md`, `.opencode/commands/cospec-update.md` and `.opencode/skills/cospec-update-change/SKILL.md` all present
- [x] 13.2 @integration (agent) run `cospec doctor` and `cospec schemas` against that project -> `cospec doctor` in that scratch project reports `0 error(s), 0 warning(s), 1 info` (the info is the unrelated openspec global profile note) with no dangling workflow or skill reference for `update`; `cospec schemas` still lists exactly 11 project schemas. In this repo `cospec doctor` reports `0 error(s), 1 warning(s), 1 info` — the warning is the pre-existing `gate-hooks` "git hooks are not installed" notice, not a dangling ref
- [x] 13.3 @unit (agent) `mise run generate` then `mise run generate:check` -> `cospec update --check: no drift`; `bun test test/unit/harness/render.test.ts` 20 pass / 0 fail with the workflow count updated from 11 to 12 across all three harnesses, and `test/unit/harness/dangling-refs.test.ts` 3 pass

## 14. Generated agent prose is runtime-neutral and correctly gated [critical]

- [x] 14.1 @unit (agent) scan every rendered command and skill body across the claude, codex, and opencode outputs -> `bun test test/unit/harness/render.test.ts` 20 pass / 0 fail: the `runtime-neutral prose` test finds no `AskUserQuestion` and no `TodoWrite` in any rendered claude/codex/opencode body, and no body carries a bare `openspec ` invocation or a hand-`mv`/`mkdir` of an `openspec/changes/` entry
- [x] 14.2 @unit (agent) read the rendered `explore`, `bulk-archive`, `apply`, `continue`, `verify`, `sync-specs`, and `archive` bodies -> same file, per-body assertions all pass: explore gates its first write on explicit consent in a separate message and reads `openspec/config.yaml`; bulk-archive carries a declined branch and does not auto-select; continue/verify/sync-specs/archive auto-select a sole active change and announce `Using change: <slug>`; apply requires surfacing added scope instead of narrowing it; sync-specs and archive describe the capability-retirement procedure
- [x] 14.3 @integration (agent) run `cospec instructions specs --change <id> --store <id>` after the canon artifact pass -> ran `cospec instructions specs --change openspec-1-11-parity` here: the emitted guidance names `<planningHome.root>/openspec/specs/<capability-path>/spec.md` and says `planningHome.root` comes from the `cospec instructions … --json` response — store-relative, not a bare CWD-relative path. Recorded honestly: the phrasing is static canon prose, identical with or without `--store`, and no dedicated `--store` integration test was authored (W15's task text corrected to say so)
- [~] 14.4 @eval (agent) `mise run eval:e2e` against the regenerated skills -> defer: `mise run eval:e2e` is the advisory DeepSeek harness and is explicitly never a CI gate; it needs a live API key from the process environment, which this pass does not have. No regression signal was collected for the prose pass

## 15. OpenCode commands stop dropping their arguments [critical]

- [x] 15.1 @unit (agent) run the injector against a body that already references `$ARGUMENTS`, a body with CRLF endings, and a body with no positional input -> `bun test test/unit/harness/adapters.test.ts` and `render.test.ts` 20 pass / 0 fail: `injectOpenCodeArgs` no-ops on a body already carrying `$ARGUMENTS` or `$1`, preserves `\r\n` line endings, appends when there is no heading, and injects nothing where the workflow takes no arguments
- [x] 15.2 @integration (agent) inspect every regenerated `.opencode/commands/cospec-*.md` for arg-taking workflows, and diff the claude and codex outputs before and after -> in the fresh scratch init, exactly two of the twelve opencode commands lack `$ARGUMENTS` — `cospec-bulk-archive.md` and `cospec-onboard.md`, the two workflows audited as taking no positional input — and every other arg-taking command carries it; the claude and codex outputs contain no `$ARGUMENTS` at all, and `mise run generate:check` reports no drift
- [~] 15.3 @manual (human) invoke `/cospec-new feat some-slug` in a real OpenCode session against a project initialized by the regenerated cospec -> defer: needs a human at an OpenCode client; the rendered artefact was verified instead (15.2)

## 16. Leftover vanilla-openspec artifacts under .agents are found, and nothing else is deleted

- [x] 16.1 @integration (agent) run `cospec doctor` then `cospec init --remove-opsx` against a fixture containing `.agents/skills/openspec-propose/SKILL.md` -> `bun test test/unit/init/doctor.test.ts` (a leftover under `.agents/skills/` is a WARNING for openspec >=1.8's Codex root), `test/unit/init/init.test.ts` (the same leftover is found and removed) and `test/integration/init.test.ts` 7 pass (state C `--remove-opsx` clears the `.agents/skills/` openspec leftover too) all pass, with the emptied directory pruned
- [x] 16.2 @integration (agent) run the same removal against a `.agents/skills/` file that is not openspec-authored, and run `cospec init` on a clean project -> the unit cases "a `.agents/skills/` file cospec or a third party wrote is never removed" and "user-authored path-matching files are never removed (provenance-only)" both pass; and the fresh scratch init above wrote no `.agents` directory at all

## 17. No wrapped first-run notice ever reaches a cospec user

- [x] 17.1 @unit (agent) inspect the wrapped-binary spawn environment -> `bun test test/unit/core/openspec.test.ts` 18 pass / 0 fail asserting `WRAPPED_ENV` is exactly `{NO_COLOR:'1', BUN_BE_BUN:'1', OPENSPEC_TELEMETRY:'0', OPENSPEC_NO_COMPLETIONS:'1'}`, and `test/contract/version-tripwire.test.ts`'s `the pinned binary still reads the env gates cospec forces on every spawn` finds both names in the pinned package's shipped dist JS — so an upstream rename fails here rather than leaking a notice
- [x] 17.2 @integration (agent) run a cospec command that relays wrapped stderr, with a HOME carrying no prior openspec global config -> ran `cospec list` with `HOME` and `XDG_CONFIG_HOME` pointed at a fresh empty temp directory: exit 0, stderr completely empty (zero matches for `completion` or `telemetry`), stdout carrying only the change listing

## 18. The contract suite describes the real 1.11.0 binary honestly [critical]

- [x] 18.1 @integration (agent) run `mise run test:contract` against real 1.11.0 BEFORE editing any test and record every failure verbatim -> the pre-edit run was performed by the implementing track (5 contract + 2 integration failures) and cannot be reproduced now that the narratives are rewritten; its record is transcribed here. Behavioural deltas: (a) the `- Scenario removed: <reason>` escape hatch is dead upstream — the wrapped ARCHIVE itself refuses (`widgets MODIFIED failed for header "### Requirement: Widget rendering" - current spec contains scenario(s) not present in the modified block: "Render an empty widget". … Aborted. No files were changed.`, exit 1); (b) an aborted archive now exits 1 from 1.7.0 (it exited 0 through 1.6.x) while `Aborted. No files were changed.` and the unmoved directory are unchanged; (c) 1.7.0 makes an identical ADDED block an early-sync no-op (`Totals: + 0, ~ 0, - 0, → 0` / `Specs already in sync; no files changed.` at exit 0, `moved=true`); (d) the binary stamps a LOCAL date prefix (`archived as '2026-09-01-added-already-exists'` while UTC said `2026-09-02`); (e) store diagnostics churn is invisible to cospec because `commands/store.ts` renders on `severity`/`message` with no code allow-list; (f) `CHANGE_NO_DELTAS` still exits 1 and 1.10.0 only appends one guidance sentence about `skip_specs`; (g) the legacy schema lane is unchanged on success, with 1.9.0's transactional `schema fork` a strictly better failure path
- [x] 18.2 @integration (agent) re-run `mise run test:contract` after rewriting the affected narratives -> 35 pass / 0 fail across 7 files against the real pinned 1.11.0 binary; each rewritten test asserts the new observed behaviour (the "a `Scenario removed:` note no longer excuses the drop" case is an explicit refusal test, not a relaxed one), and cospec's own `archive/scenario-preservation` gate still refuses BEFORE delegating — it remains the sole defence on runtimes 1.0.0–1.7.x
- [~] 18.3 @integration (agent) run the newly added `schemas --store` and `show --diff` relay tests against the real binary -> defer: those two relay tests were never authored — track A recorded the hand-off (both land in files another track owned) and no track picked it up. No behaviour is unshipped: `cospec schemas` and `cospec show` are generic `runPassthrough` surfaces that already thread `--store` and forward `--diff` verbatim, and a live `cospec show <change> --diff` here relays at exit 0. Recorded as a follow-up and written into W19's corrected task text
- [x] 18.4 @regression (agent) run `mise run test:integration` and `mise run test` after the fixture fixes forced by the 1.8/1.9 delegated rules -> `mise run test:integration` 113 pass / 0 fail across 20 files and `mise run test` 659 pass / 0 fail across 43 files; the fixtures were corrected rather than the delegated rules filtered out

## 19. cospec apply --json reports one task total, not two

- [x] 19.1 @integration (agent) contract test running `cospec apply --json` against the real binary on a `tasks.md` fixture with nested sub-task checkboxes -> `bun test test/integration/apply-surfaces.test.ts` (in the 113 pass integration run): `a nested checkbox is blocked at fast validation, before the wrapped call ever runs` shows `tasks/checkbox-grammar` in `issues` with no `progress` key in stdout, and a flat `tasks.md` exits 0 with `apply.progress.total`/`complete` matching `parseTasks(tasksText)` exactly. The documented counting rule matches: the two counters can never disagree on a change that reaches the clear-gate JSON, so no counting-rule code change was made (W21's task text corrected to say so)

## 20. Published docs and shared agent prose carry no drift

- [x] 20.1 @integration (agent) `mise run docs:build` and `mise run agents:check` after the docs sweep -> `docs:build` exits 0 (`build complete`, client+server bundles, pages rendered, sitemap generated); `agents:check` reports `OK` for both `CLAUDE.md` and `AGENTS.md` and `All shared blocks are in sync.`
- [x] 20.2 @unit (agent) grep the repo for `1.5.0`, "eleven workflows", and the `/opsx:update` deferral bullet -> zero files under `docs/`, `apps/docs/` (sources), `README.md`, `apps/cli/README.md` and `.agents/` still contain a `1.5.0` literal; zero matches for "eleven workflows"/"11 workflows"; the workflow count now reads "twelve" in `docs/architecture.md` and `apps/docs/guide/harness-setup.md`; the `/opsx:update` deferral bullet is gone, replaced by `/opsx:update` maps to `/cospec:update`; the schema count still reads "eleven schemas" in `docs/schemas.md`, `apps/docs/concepts/types-and-artifacts.md`, `apps/docs/concepts/how-it-relates-to-openspec.md` and `README.md`; and `apps/docs/reference/commands.md` carries rows for `status --all`, `validate --archived`, `instructions archive`, `show --diff`, `apply --skip-specs` and archive's `warnings`/`retired` arrays, plus the per-surface runtime minimums (>=1.7.0, >=1.9.0)

## 21. The whole repository gate passes with the new pin and surfaces [critical]

- [x] 21.1 @runtime (agent) `mise run check` on the final branch state -> exit 1, from exactly the two pre-existing failures this branch inherited from `main` and must not fix: `//e2e:release-test`'s `release:set-version > stamps the target version into apps/cli/package.json` (asserts `stderr === ''`, broken by a mise `experimental_monorepo_root` deprecation warning) and `//packages/bench:test`'s `test/unit/hidden.test.ts` (2 failures — `SCENARIOS` names a `build` scenario with no `scenarios/hidden/build/` directory). Everything this change touches is green in the same run: typecheck exit 0, lint exit 0 (the same 3 pre-existing unicorn warnings as `main`, in untouched files), `format:check` clean across 684 files, unit 659 pass / 0 fail, contract 35 pass / 0 fail, integration 113 pass / 0 fail, pack smoke 2 pass, `generate:check` `no drift`, `vendor:openspec:check` up to date, `agents:check` in sync, all 11 schemas valid, and `cospec validate --all --strict` `0 errors, 0 warnings — validation passed`
- [~] 21.2 @manual (human) exercise `cospec status --all`, `cospec validate --archived`, `cospec instructions archive`, and an archive that emits warnings in a real terminal -> defer: reading the human-mode blocks, issue rendering, artifact payload and warning lines needs a human at a terminal. The machine-checkable half was observed instead: the `--all --json` envelope, the mutex error in both modes, and the rendered warning/retirement lines are all asserted by the tests cited in rows 5.1, 6.1, 9.1 and 10.2, and row 14.1 proves no generated body names a bare `openspec` command
