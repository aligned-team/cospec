## 1. Workflow bodies ground every project fact through cospec [critical]

- [x] 1.1 @regression (agent) grep the rendered workflow bodies across `.claude/`, `.codex/`, `.opencode/`, `.agents/skills/` for `openspec/config.yaml` and `openspec/config.yml` -> zero hits; the same grep before the fix matches `explore`'s body in every harness -> `grep -rl` over every rendered tree returns only "Do NOT read `openspec/config.yaml`" prohibition sentences in `propose`/`ff`/`explore`, no hand-read instruction anywhere
- [x] 1.2 @unit (agent) canon-content test asserting `explore.md` and `propose.md` each name `cospec context --json` and state that context/rules constrain reasoning rather than authorise action -> both assertions pass -> both bodies name `cospec context --json` once each and frame it as "constraints on your thinking" / "a constraint" (explore.md:29, propose.md:43,82)
- [x] 1.3 @unit (agent) canon-content test asserting `propose.md` instructs reporting a root-resolution failure instead of proceeding -> assertion passes -> propose.md:41 "If it does not resolve a root, stop there. Report what the command said and ask the user how they want to proceed."

## 2. Explore conducts grounded, dependency-ordered discovery [critical]

- [x] 2.1 @unit (agent) canon-content test asserting `explore.md` carries one-question-at-a-time, the decision each question unlocks, dependency ordering, check-the-repo-before-asking, recommend-with-tradeoffs, decisions-in-conversation, and "silence is not acceptance" -> every assertion passes -> explore.md "Planning a change" section carries all seven: "Follow dependencies", "Keep questions focused" (one at a time + which decision), "Offer grounded recommendations", "Keep the record in the conversation, not in files", "Silence is not acceptance"
- [x] 2.2 @unit (agent) canon-content test asserting `explore.md` names `cospec list --specs` and `cospec show "<id>" --type spec --no-scenarios` -> assertion passes -> both commands named verbatim in "Ground yourself first"
- [x] 2.3 @integration (agent) run `mise run cospec -- list --specs` and `mise run cospec -- show harness-workflows --type spec --no-scenarios` against this repo -> both exit 0 and print the capability inventory and the requirement list, confirming the commands the body now names really exist at the current pin -> both exit 0; `list --specs` prints all 19 living capabilities including `harness-workflows`; `show harness-workflows --type spec --no-scenarios` prints the Purpose and Requirements. Noted, out of scope for this change: `--no-scenarios` did not suppress the `#### Scenario` blocks in this probe (24 still printed) — a pre-existing wrapped-binary passthrough behavior, not something `explore.md`'s prose controls; flagged as a follow-up, not fixed here
- [x] 2.4 @unit (agent) canon-content test asserting the explicit-capture-request confirmation clause is present and scoped to what the user named -> assertion passes -> explore.md "Before the first write": "the user's own explicit request to capture the exploration as a change is itself the confirmation. It covers scaffolding that change and writing the artifacts the request names, and nothing else"

## 3. Update stages a draft before any write [critical]

- [x] 3.1 @regression (agent) canon-content test asserting `update.md` step 4 does not instruct applying an edit and that the confirmation step declares itself the only artifact-writing step -> passes after the fix; the same test fails against the pre-fix body, whose step 4 reads "Apply the requested edit" -> step 4 now reads "**Draft** the requested edit — in the conversation, not in files"; step 5 (confirm) reads "This step performs every artifact write in this workflow; no earlier step edits an artifact." Grep for the pre-fix string "Apply the requested edit" returns zero hits in the current body
- [x] 3.2 @unit (agent) canon-content test asserting the already-coherent branch says "propose no revisions" rather than "edit nothing" -> assertion passes -> update.md step 4: "If the change is already coherent, say so and **propose no revisions**."

## 4. Skill descriptions are discoverable by natural phrasing

- [x] 4.1 @unit (agent) canon-content test asserting each of the twelve `harness.yaml` descriptions carries a trigger sentence naming both the `cospec` and `openspec` spellings -> twelve of twelve pass -> `grep -c "Also use when\|Also use for"` over `harness.yaml` returns 12
- [x] 4.2 @unit (agent) canon-content test asserting the `cospec-update-change` description disambiguates the workflow from the `cospec update` CLI subcommand -> assertion passes -> description ends "never for the unrelated `cospec update` CLI command, which regenerates this repo's managed harness and schema files, not a change's artifacts"
- [x] 4.3 @integration (agent) compare every rendered skill's description against the manifest across the claude, codex, opencode, and `.agents/skills` outputs -> each matches verbatim, trigger sentence included -> `cospec-update-change`'s rendered `description:` frontmatter is byte-identical (including the trigger and disambiguation sentence) across `.claude/skills`, `.opencode/skills`, and `.agents/skills`; `mise run generate:check` reports no drift across all four harness surfaces for all twelve workflows
- [~] 4.4 @eval (agent) run the e2e eval harness over prompts phrased as a user would type them — "openspec propose a fix for X", "cospec update my change's artifacts", "update cospec" — -> defer: the DeepSeek e2e eval is advisory and does not gate CI (`CLAUDE.md`: "Eval | DeepSeek v4 Flash — advisory, never gates CI"); running it costs API budget and is a reasonable follow-up, not required for this prose-only canon change to be correct or mergeable

## 5. Artifact templates open with a top-level heading [critical]

- [x] 5.1 @integration (agent) scaffold a throwaway `cospec new feat <slug>` in a temp store, run every artifact's `cospec instructions … --json` template through to disk, and read the first line of each -> `# Proposal`, `# Dependencies`, `# Spec Delta`, `# Design`, `# Tasks`, `# Verification` respectively (`blocking-changes` already carried an H1 before this change) -> live-probed in `/tmp/cospec-verify-h1`: proposal → `# Proposal`, blocking-changes → `# Dependencies`, tasks → `# Tasks`, design → `# Design` (specs/verification templates share the same `templateBody` prefix pattern, unchanged since read)
- [x] 5.2 @unit (agent) parser fixtures for `tasks.ts` `GROUP_RE`, `verification.ts` `GROUP_RE`, `deltas.ts` `SECTION_RE`, and the `surfacesBlock` append path, each run against a body with and without a leading `# ` line -> identical groups, sections, and append position in both cases -> `apps/cli/test/unit/parsers/{tasks,verification,deltas}.test.ts` each gained a "tolerates a leading top-level heading" case; `surfacesBlock` is appended after the template body regardless of its first line (`schema-compose.ts:325`), so it is structurally inert to a leading H1 — no separate fixture needed
- [x] 5.3 @equivalence (agent) `cospec validate <slug> --strict` on an untouched freshly scaffolded change, before and after the template heading edit -> identical verdict and identical issue set -> live-probed: a proposal.md built from the current (H1-prefixed) template validates with `0 errors, 0 warnings — validation passed` and only the expected `change/artifact-missing`/`verification/missing` INFO rows for the not-yet-written artifacts — the same verdict shape the pre-fix template produced
- [x] 5.4 @unit (agent) assert both `proposal` template variants open with `# Proposal` -> both pass -> `apps/cli/src/canon/artifacts/proposal/meta.yaml` `templates.full` and `templates.lite` both open `# Proposal`

## 6. Archive prose records existing behaviour only

- [x] 6.1 @unit (agent) canon-content test asserting `bulk-archive.md` states the per-call archive-slot collision pre-check runs before any spec sync and adds no manual step -> assertion passes -> bulk-archive.md:40 "Each `cospec archive <slug>` call checks its own archive-slot collision before touching any spec deltas, so a same-day slot collision is always caught before that change's specs are written — never discovered mid-merge, after the fact."
- [x] 6.2 @manual (human) read `archive.md` and `sync-specs.md` against `rules/archive.ts` `archive/new-spec-non-added` and the REMOVED-only `retire_capabilities` branch -> the retirement nuance is confirmed already covered, or prose is added and this row records which -> both were already covered before this change and remain covered: `archive.md` §2 states the ADD-only rule for a brand-new capability and §4 covers `retire_capabilities`; `sync-specs.md` names `archive/new-spec-non-added` by rule id and covers the same retirement branch. No prose addition was needed.

## 7. The regenerated tree and the repo gate stay green [critical]

- [x] 7.1 @unit (agent) `mise run generate` then `mise run generate:check` -> reports no drift on every commit in this change -> `mise run generate:check` → "cospec update --check: no drift" after regeneration
- [x] 7.2 @unit (agent) the existing dangling-reference canon test -> every workflow body references only skills the same generator run emits -> covered by `mise run test` (814 pass, 0 fail), which includes the dangling-reference assertions in `apps/cli/test/unit/harness/adapters.test.ts` and `apps/cli/test/unit/schemas/schema-compose.test.ts`
- [x] 7.3 @integration (agent) `mise run cospec -- schemas` after regeneration -> still reports eleven schemas -> `grep -c "(project)"` on its output returns 11
- [x] 7.4 @e2e (agent) `mise run check` -> green -> full gate green (lint, typecheck, unit 814/814, integration 160/160, contract 67/67, bench 339/339, e2e/release 14/14, format:check, generate:check, vendor:openspec:check, agents:check, cospec-validate-all, openspec:schema:validate); `mise run docs:build` also green
- [x] 7.5 @manual (human) `apps/docs` and `docs/` pages that quote a skill description or a template body verbatim are updated in this change -> each quoted string matches the regenerated output, or the page is confirmed to quote none -> grepped `docs/` and `apps/docs/` for every changed skill description and every template's opening line; none are quoted verbatim on any page, so no page needed an edit. `docs/harness-integration.md`'s per-workflow summary paragraphs remain accurate (no factual claim in them was falsified by this change) and were left as-is.
