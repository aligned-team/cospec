## 1. Document self-hosting

- [x] 1.1 Add a `## Self-hosting` section to `CONTRIBUTING.md` describing the
      cospec loop (new → author → validate → apply → implement → archive)
- [x] 1.2 Verify the section renders and `mise run check` stays green
