# Verification: task-marker-coverage

## 1. The two hard archive gates refuse work written with a non-canonical marker [critical]

- [x] 1.1 @regression (agent) `cospec archive` on a fixture change whose `tasks.md` mixes a completed `- [x]` row with an unfinished `+`-bulleted task -> refused. `apps/cli/test/unit/commands/archive.test.ts` ("an unfinished `+ [ ]` task blocks archive with exit 1") and the `1. [ ]` integration twin in `test/integration/archive-gates.test.ts` both observe exit 1 with the change still on disk. Honest correction to the row as written: the refusal lands in **step 2 validation** as `tasks/checkbox-grammar` naming `corrected: - [ ] 1.2 not done yet`, not in the step 3 incomplete-task list — and, unlike the tasks gate, `--force-incomplete` does not waive it (asserted). Before the fix the same fixture archived clean (integration twin archives at exit 0 once the row is canonical)
- [x] 1.2 @regression (agent) `cospec archive` on a fixture change whose `verification.md` rows are written entirely with `+` bullets -> refused at exit 1 (`archive.test.ts`, "a `+`-bulleted ledger refuses archive instead of reporting 0/0 verified"). Honest correction: the refusing rule id is `verification/row-grammar` in step 2 validation, ahead of the step 3b `archive/verification-incomplete` gate the row named. Before the fix the same ledger reported `total 0` with no blockers — the false PASS this change closes
- [x] 1.3 @unit (agent) `computeVerificationVerdict` over a ledger whose rows all use `+` bullets -> `total 0` **with** `blockedReasons: ['2 row(s) do not parse']` (previously `[]`); one `+ [ ]` row among canonical rows yields `['1 row(s) do not parse']`. `test/unit/parsers/verification.test.ts`, green

## 2. Detector coverage and the link-bullet guard

- [x] 2.1 @unit (agent) `parseTasks` over `+ [ ] 1.1 x`, `* [ ] 1.1 x`, `1. [ ] 1.1 x`, `1) [x] 1.1 x` -> each reported as `tasks/checkbox-grammar` with `corrected` equal to the canonical `- [ ] …` / `- [x] …` rewrite (the widened `CHECKBOX_PREFIX` strip). `test/unit/parsers/tasks.test.ts` + `test/unit/rules/tasks.test.ts`, green
- [x] 2.2 @unit (agent) `parseVerification` over the same four markers × `[ ]`/`[x]`/`[~]` carrying a well-formed row body -> `rows` empty, one `verification/row-grammar` malformed entry each; the canonical `- [~] … -> defer: <reason>` row still parses as `deferred` with its reason. `test/unit/parsers/verification.test.ts`, green
- [x] 2.3 @unit (agent) `parseTasks` and `parseVerification` over `- [Some doc](./doc.md)` and `- [1](./one)` -> neither parser reports a malformed line (upstream's `(?![([])` link-bullet guard, adopted with its whitespace-only-box exception so `- [ ](./x)` stays checkbox-like). This also clears a pre-existing false positive on plain doc links. Both parser suites green
- [x] 2.4 @unit (agent) `parseTasks` over the canonical `- [ ] 1.1 x` and over an indented `  - [ ] 1.1 x` -> canonical is still a parsed item; indented is still a `tasks/checkbox-grammar` ERROR. `TASK_VALID` was left untouched, so neither existing behaviour moved. Green

## 3. Rule-level and project gates

- [x] 3.1 @integration (agent) `cospec validate <fixture with a non-canonical task marker> --strict` -> exit 1 with `tasks/checkbox-grammar` and `- [ ] 1.2 Wire CI` in stdout. Asserted by the new `` `validate --strict` reports the same row before archive is ever reached `` test in `test/integration/archive-gates.test.ts` (17 pass / 0 fail)
- [x] 3.2 @unit (agent) `mise run test` -> 834 pass / 0 fail across 48 files, including every row above
- [x] 3.3 @equivalence (agent) `mise run test:contract` -> 67 pass / 0 fail across 9 files against the real pinned binary (1.11.0): unchanged and green, so the fix is pin-independent
- [x] 3.4 @integration (agent) `mise run check` -> green end to end (lint, format, typecheck, generate drift, unit 834, integration 162, contract 67, release-test 14; 0 fail anywhere). Note: run as `env -u FORCE_COLOR` — with `FORCE_COLOR` set in the shell, Bun emits a `NO_COLOR is ignored` warning on stderr that trips the pre-existing, unrelated `config set defaultStore` stderr assertion

## 4. Docs carry the widened detector set

- [x] 4.1 @manual (human) read `docs/validation.md` and `apps/docs/reference/validation-rules.md` after the edit -> both describe the widened marker set for `tasks/checkbox-grammar` and `verification/row-grammar` (`docs/validation.md` "Checkbox grammar" section; `validation-rules.md` `tasks/` and `verification/` tables), and neither still implies only `-`/`*` are detected
- [x] 4.2 @integration (agent) `mise run docs:build` -> build complete in 1.15s, no errors, `llms.txt`/`llms-full.txt` regenerated

## 5. The same marker gap is closed in the two sibling detectors [critical]

- [x] 5.1 @regression (agent) `cospec apply` on a change whose `## Blocked by` holds an unchecked, unarchived widened-marker `` [ ] `dep` `` entry -> exit 1 reporting `blockers/entry-grammar`, asserted for all four markers (`*`, `+`, `1.`, `1)`) in `test/unit/commands/apply.test.ts`. Honest correction: the refusal comes from step 2 fast validation (`fast` skips only the archive family, so `blockersRules` runs), not from `computeGate` — `computeGate` is unchanged and still reads parsed entries. Before the fix the entry parsed as nothing, `computeGate` returned `{state: 'clear', hard: []}`, and apply exited 0 over a real unshipped dependency
- [x] 5.2 @unit (agent) `parseBlockers` over `*`/`+`/`1.`/`1)`-bulleted gated-section entries and over the same markers on a backticked-slug bullet outside the sections -> each is a malformed entry carrying the canonical `corrected` line, or a linted loose slug bullet (`LOOSE_SLUG_BULLET` widened on the same line of reasoning, since it is the second net under `CHECKBOX_LIKE`); the canonical `- [ ] ` entry still parses as an entry. `test/unit/parsers/blockers.test.ts` + `test/unit/rules/blockers.test.ts`, green
- [x] 5.3 @unit (agent) `parseSurfaces` / `checkedSurfaces` over `+ [x] deploy`, `1. [x] interactive` and `+ [x] telepathy` -> each flag is read with its token and checked state, the unknown token reaches `proposal/surfaces-vocab`, and `999999999.` is read while a 10-digit marker is not. `test/unit/parsers/proposal.test.ts`, green. Before the fix each line was skipped with no diagnostic, silently unsetting the flag
- [x] 5.4 @manual (human) read `docs/validation.md` and `apps/docs/reference/validation-rules.md` after the edit -> both state the shared marker set for `blockers/entry-grammar` and the `## Surfaces` reader, and name the gate consequence rather than describing it as lint (`docs/validation.md` "Checkbox grammar" section covers both; `validation-rules.md` `blockers/entry-grammar` row + note)
