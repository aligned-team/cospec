# Verification

## 1. Case-variant requirement headers reach the operation list [critical]

- [x] 1.1 @regression (agent) parse a `## ADDED Requirements` section whose block is `### requirement: Lower` and a second whose block is `### REQUIREMENT: Upper`, before and after the fix -> before: `ops` is empty and `emptySections` contains `ADDED`; after: two ADDED ops with names `Lower` and `Upper` and their scenario counts -> `apps/cli/test/unit/parsers/deltas.test.ts` `'requirement header keyword case' > 'ADDED reads lower-, upper- and mixed-case keywords'` passes
- [x] 1.2 @regression (agent) parse `## REMOVED Requirements` carrying `### requirement: Alpha` -> before: no op, `emptySections` contains `REMOVED`; after: one REMOVED op named `Alpha`, so `archive/target-missing` can see it -> `deltas.test.ts` `'REMOVED reads a case-variant plain header'` passes; gate-level coverage in `apps/cli/test/unit/rules/archive.test.ts` `'archiveRules: case-variant requirement headers reach the gates'`
- [x] 1.3 @regression (agent) parse `## MODIFIED Requirements` carrying `### REQUIREMENT: Alpha` -> before: no op; after: one MODIFIED op named `Alpha` carrying its scenario names, so `archive/scenario-preservation` can compare it -> `deltas.test.ts` `'MODIFIED reads a case-variant keyword and keeps its scenario names'` passes

## 2. The mixed-section absorption is gone [critical]

- [x] 2.1 @regression (agent) parse one `## MODIFIED Requirements` section holding `### Requirement: Alpha` (scenario `s1`) followed by `### requirement: Beta` (scenario `s2`) -> before: exactly one op named `Alpha` with `scenarioCount` 2 and `scenarioNames` `['s1','s2']` — Beta invisible and Alpha's scenario set inflated; after: two ops, `Alpha`/`['s1']` and `Beta`/`['s2']` -> `deltas.test.ts` `'a case-variant header closes the block above it instead of joining it'` passes
- [x] 2.2 @integration (agent) `cospec archive` a change whose MODIFIED block is `### requirement: Alpha` and drops a scenario the living spec still has -> `archive/scenario-preservation` refuses (exit non-zero, change unmoved, living spec byte-identical). Before the fix the gate sees no op for Alpha and the binary applies the drop — the false archive PASS this change exists to close -> `apps/cli/test/unit/rules/archive.test.ts` `'archive/scenario-preservation refuses a drop under a lowercase header'` passes at the rule level; `apps/cli/test/contract/fixtures.ts` fixture `lowercase-header-modified-drops-scenario` (rule `archive/scenario-preservation`, `expectAbort: true`) runs the real pinned binary via `archive-parity.test.ts` -> green

## 3. Nothing else loosens

- [x] 3.1 @regression (agent) parse a lowercase REMOVED **bullet** (``- `### requirement: Alpha` ``) and lowercase `from:`/`to:` rename lines -> still no ops, matching the binary; the fix touches only the canonical header -> `deltas.test.ts` `'the REMOVED bullet form stays case-sensitive, as the binary is'` and `'FROM:/TO: rename keywords stay case-sensitive, as the binary is'` pass
- [x] 3.2 @equivalence (agent) contract test spawning the REAL pinned 1.13.1 binary on the probe matrix from the proposal -> lowercase/uppercase `### Requirement:` under ADDED/MODIFIED/REMOVED parse (`show --json --deltas-only` `deltaCount` > 0, `archive` really applies); the REMOVED bullet and the `FROM:`/`TO:` forms in lowercase do NOT parse (`deltaCount` 0, validate reports "no deltas found"). Pins the asymmetry so a pin bump that changes it fails loudly -> `apps/cli/test/contract/fixtures.ts` fixtures `lowercase-header-modified` (`expectAbort: false`) and `lowercase-header-modified-drops-scenario` (`expectAbort: true`, rule `archive/scenario-preservation`) run through `archive-parity.test.ts` against the pinned 1.13.1 binary; `mise run test:contract` -> 120 pass, 0 fail
- [x] 3.3 @unit (agent) confirm no second regex needed widening -> `SCENARIO_RE` is `/^####\s+/` (upstream `SCENARIO_HEADER` identical, so `#### scenario:`/`#### SCENARIO:` already parse) and delta section titles are lowercased before the `SECTION_TITLES` lookup (upstream `getSectionsCaseInsensitive`) -> confirmed by reading `apps/cli/src/core/deltas.ts`; no code change needed beyond `REQUIREMENT_RE`; `REMOVED_BULLET_RE`/`RENAMED_FROM_RE`/`RENAMED_TO_RE` deliberately left case-sensitive (comment records why)

## 4. Suite and docs

- [x] 4.1 @unit (agent) `mise run test` -> green: 983 pass, 0 fail, 4286 expect() calls across 50 files
- [x] 4.2 @equivalence (agent) `mise run test:contract` against the pinned binary -> green: 120 pass, 0 fail, including the new case-matrix fixtures
- [x] 4.3 @manual (agent) the `apps/docs` page that owns the delta header format, plus `docs/validation.md` -> each states the header-keyword case tolerance and its limits exactly once, with no fact duplicated across the two -> `apps/docs/reference/validation-rules.md` states the full tolerance and its limits (site-owned fact); `docs/validation.md` states only the mechanism (keyword folded before matching) and links out to the site page for the authoring-facing statement — no duplicated fact
- [x] 4.4 @integration (agent) `mise run check` -> green end to end
