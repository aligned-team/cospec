## 1. WU-1 delta bullet markers are recognised [critical]

- [x] 1.1 @regression (agent) run the new `deltas.test.ts` bullet fixtures — `*`- and `+`-bulleted REMOVED, an indented ` -` FROM/TO pair, a `*` bullet inside a fence — against the parser before and after the fix -> before: the `*`/`+`/indented cases record no op; after: each records its op and the fenced bullet still records none -> `bun test apps/cli/test/unit/parsers/deltas.test.ts`: 96 pass / 0 fail; `describe('delta bullet markers')` records an op for `-`/`*`/`+` REMOVED, for `-`/`*`/`+` and unbulleted FROM/TO, and for ` -`/`\t* `/`  +` indented forms in both sections, while `bulleted delta lines inside a fence are still ignored` leaves `ops` empty and `emptySections` `['REMOVED','RENAMED']` for both a ```and a`~~~` fence
- [x] 1.2 @regression (agent) `cospec validate --strict` a change whose `## REMOVED Requirements` names a near-miss of a living requirement (`Widget  caching` vs `Widget caching`) with a `*` bullet -> `archive/target-missing` ERROR, exit 1 (before WU-1 the entry never parsed, so the gate reported `archive/no-ops` instead — never a clean exit 0; an absent-not-near-miss target is upstream's already-removed early sync and is correctly silent on both sides) -> exit 1, `ERROR specs/widgets/spec.md:3 archive/target-missing  REMOVED target "Widget  caching" does not exist in living spec openspec/specs/widgets/spec.md`, alongside the relayed pin refusal `ERROR … openspec/validate  Delta sections ## REMOVED Requirements were found, but no requirement entries parsed`
- [x] 1.3 @integration (agent) `cospec archive` that same change -> refused at the `archive/target-missing` pre-flight before any delegation -> exit 1, the only diagnostic printed is `ERROR specs/widgets/spec.md:3 archive/target-missing …`; `openspec/changes/near-miss` still on disk, living spec untouched
- [x] 1.4 @equivalence (agent) `apps/cli/test/contract/delta-bullet-markers.test.ts` — spawn the REAL pinned 1.11.0 binary on a `*`- and a `+`-bulleted REMOVED and RENAMED, and on the indented-`-` controls -> the pin REFUSES `*`/`+` (`… but no requirement entries parsed`, archive exit 1, change unmoved, living spec untouched) and ACCEPTS the indented `-`; cospec relays the refusal as `openspec/validate` and never moves the change, so the marker widening leads the pin without producing a false archive PASS -> `bun test apps/cli/test/contract/delta-bullet-markers.test.ts`: 6 pass / 0 fail against the real pinned binary

## 2. WU-2 unpaired rename lines are refused

- [x] 2.1 @regression (agent) parse a `## RENAMED Requirements` section ending in a lone `FROM:` -> no RENAMED op is recorded, the dangling line is retained with side/name/line, and `deltas/unpaired-rename` is reported as an ERROR naming the missing `TO:` -> `bun test apps/cli/test/unit/parsers/deltas.test.ts apps/cli/test/unit/rules/deltas.test.ts`: `a FROM: with no TO: at EOF records no op and reports the stray` and `describe('deltas/unpaired-rename')` both pass; the ERROR reads `RENAMED FROM: "<name>" has no matching TO: line`
- [x] 2.2 @unit (agent) parse `FROM: a`, `FROM: b`, `TO: x` in one section -> exactly one RENAMED op (`a`→`x` is not manufactured from the wrong pair), exactly one unpaired `FROM:` reported, and `b` is not renamed to `x` -> `two FROM: lines then one TO: pair only the second — the first is reported` passes: one op `b`→`x`, one unpaired `FROM: a`
- [x] 2.3 @regression (agent) parse a section whose only content is a dangling `FROM:` -> the section is reported in `emptySections`, which the phantom op previously suppressed -> asserted in the same describe; `emptySections` contains `RENAMED` and `ops` is empty
- [x] 2.4 @unit (agent) parse a `FROM:` followed by a new `## ` section header carrying a `TO:` -> the two do not cross-pair; both are reported unpaired -> `a FROM: is closed by the next \`## \` header, not carried into it`and`a pair split across two copies of the RENAMED header does not pair` both pass

## 3. WU-3 requirement names ignore a closing ATX run

- [x] 3.1 @regression (agent) `cospec validate --strict` a delta writing `### Requirement: Foo ###` against a living `### Requirement: Foo` -> no `archive/target-missing`; the names resolve to one requirement -> `cospec validate trailing-hashes --strict` on `buildTrailingHashesModified`: exit 0, zero errors and zero warnings, no `archive/target-missing`. Portability caveat, probed and now documented: the pinned 1.11.0 binary still captures the run into the name and aborts the delegated archive, which `cospec archive` reports because it verifies the move on disk (`archive-gotchas.test.ts`, `a trailing ### header: 1.11.0 aborts (1.13.1 applies)`)
- [x] 3.2 @unit (agent) normalise `### Requirement: C#` and a name ending in a non-space-preceded `#` run -> the `#` is preserved, proving the `[ \t]+#+[ \t]*$` class rather than `\s` -> `describe('normalizeRequirementName')`: `keeps a \`#\` that is part of the name`and`only a space- or tab-preceded run closes: an NBSP run stays in the name` both pass
- [x] 3.3 @unit (agent) normalise `### Requirement: Foo ###` on the living-spec side and fold it with `foldRequirementName` -> both yield `Foo` / `foo`, so near-miss detection inherits the strip -> `foldRequirementName inherits the strip` passes, as do the delta-side/living-side pair in `describe('closing ATX runs in requirement headers')`

## 4. WU-4 bodyless scenarios count on neither side [critical]

- [x] 4.1 @regression (agent) `cospec validate --strict` a MODIFIED block whose only scenario is a bare `#### Scenario: X` header -> `deltas/requirement-shape` ERROR carrying the new hint naming the missing body -> exit 1, `ERROR specs/widgets/spec.md:3 deltas/requirement-shape  MODIFIED "Widget rendering" must include at least one #### Scenario:` with `hint: a scenario header with no body under it does not count; add its steps, e.g. "- **WHEN** ..." and "- **THEN** ..."`
- [x] 4.2 @regression (agent) `cospec archive` a delta that replaces a real living scenario with a bare header -> `archive/scenario-preservation` refuses with exit 1 (today: false PASS) -> exit 1; the pre-flight prints `ERROR … deltas/requirement-shape` plus `WARNING … archive/scenario-preservation  MODIFIED "Widget rendering" drops scenario count from 1 to 0`, and `openspec/changes/hollow-archive` is still on disk. The refusal is real; the arm that fires first is the shape ERROR, not the preservation gate — the gate's own count arm is what records the 1 -> 0 drop
- [x] 4.3 @regression (agent) `cospec archive` a change whose _living_ spec carries a bodyless scenario header the delta does not repeat -> the gate reports no loss and does not refuse (today: false refusal), proving both counters are gated -> **prediction refuted, and the refutation is the evidence.** `cospec archive phantom` exits 1: `"Widget rendering" 1 -> 1 scenario(s); missing: "Bodyless leftover"`. The count arm IS gated (living 1, delta 1 — no count drop); the NAME arm is deliberately not, and the same fixture handed to the REAL pinned 1.11.0 binary refuses identically — `openspec validate --strict` exit 1 (`MODIFIED "Widget rendering" omits scenario(s) the current spec still has: "Bodyless leftover"`) and `openspec archive -y` exit 1 (`Aborted. No files were changed.`, change unmoved, living spec byte-identical). Gating the name arm would have made cospec quieter than the binary it wraps and traded cospec's rule id for the delegated `openspec/validate` twin. Recorded as an intentional divergence from this row's prediction, not a defect
- [x] 4.4 @unit (agent) parse a bodyless `#### ` header -> it contributes neither a `scenarioCount` increment nor a `scenarioNames` entry, so the count arm and the name-identity arm of the gate agree -> half confirmed, half corrected by 4.3: `describe('bodyless scenario headers')` (11 tests, all passing) asserts no `scenarioCount` increment on either side, and asserts that the name IS still recorded — the asymmetry openspec itself has (`countScenarios` filters on `hasScenarioBody`; `parseScenarioBlocks`, which feeds the scenario-loss check, does not)

## 5. WU-5 orphaned requirement blocks are reported

- [x] 5.1 @regression (agent) `cospec validate` a delta carrying a `### Requirement:` block above the first `## ` header and one under `## Notes` -> two `deltas/orphaned-requirement` WARNINGs naming each requirement (today: both discarded silently) -> `WARNING specs/widgets/spec.md:1 deltas/orphaned-requirement  requirement "Stray above" is above the first "## " section, which is not a delta section, so it is ignored` and `WARNING specs/widgets/spec.md:18 … requirement "Stray in notes" is under "## Notes", …`
- [x] 5.2 @unit (agent) run the same fixture non-strict -> the exit code is unchanged, confirming WARNING not ERROR -> non-strict exit 0 with both warnings printed; the same run with `--strict` exits 1, so the level is WARNING and only `--strict` promotes it

## 6. Suite, pin-independence and docs

- [x] 6.1 @unit (agent) `mise run test` -> green; all five WU fixture families pass and the coverage gate holds -> 871 pass / 0 fail, 6 snapshots, 3876 expect() calls across 48 files
- [x] 6.2 @equivalence (agent) `mise run test:contract` against the pinned 1.11.0 binary -> green, including the new `delta-bullet-markers` fixtures. WU-2/WU-5 are pin-independent; WU-1's leading-whitespace half is catch-up to the pin but its `*`/`+` marker class is 1.13.1-only and is evidenced by 1.4, not assumed; WU-3 and WU-4 are recorded as anticipating 1.13.1 semantics and are re-asserted against the real binary in the pin-bump change -> 77 pass / 0 fail across 10 files. Note for a future runner: the suite's `config-surface` probe asserts an empty stderr, so it fails under an inherited `FORCE_COLOR` (Bun prints `The 'NO_COLOR' env is ignored…`); run it with `FORCE_COLOR` unset — unrelated to this change, reproducible on `main`
- [x] 6.3 @unit (agent) parse one delta file repeating `## MODIFIED Requirements` twice and once in a different letter case -> every section's requirements accumulate, recording the single-forward-pass property the upstream unified-reader fix restores elsewhere -> `describe('every matching delta section applies')` → `repeated and case-variant section headers both accumulate` passes
- [x] 6.4 @manual (agent) `docs/validation.md` and `apps/docs/reference/validation-rules.md` -> both list `deltas/unpaired-rename` and `deltas/orphaned-requirement` and state the widened delta bullet forms, with no other fact duplicated across the two pages -> amended to respect the one-fact-one-page rule: the site's registry table owns both rule rows (level + check); `docs/validation.md` owns parser tolerance and names the two ids only in that prose. Both pin caveats found missing during this pass were added there in the same commit — a `*`/`+` bullet is refused by the 1.11.0 relay, and a closing ATX run still aborts the delegated archive at the pin — so neither page claims a portability the pinned binary does not deliver
- [x] 6.5 @integration (agent) `mise run check` -> green end to end -> exit 0; lint, oxfmt, tsgo, `generate:check`, `agents:check`, docs build, unit (871), contract (77), integration (160), bench (339) and release-test (14) all pass
