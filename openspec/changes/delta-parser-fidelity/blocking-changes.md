# Dependencies

## Blocked by

<!-- Scanned every directory under openspec/changes/: this change is the only
     active one. openspec/changes/archive/ was scanned for providers; the
     parser, its rules module and its test files are all already in place, so
     nothing archived is a prerequisite. -->

None.

## Soft-blocked by

<!-- `fix: task-marker-coverage` is planned alongside this change and shares
     its motivation (a silent parser drop feeding a hard archive gate), but it
     edits `core/tasks.ts` and `core/verification.ts` only — disjoint files,
     disjoint rules, no shared fixture. Neither change degrades without the
     other. -->

None.
