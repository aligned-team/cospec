# Verification

## 1. The pin moves cleanly and the vendored bundle follows it [critical]

- [x] 1.1 @integration (agent) `mise install && git diff --exit-code mise.lock` -> exits 0 with no diff: re-run on the committed tree, `mise install` then `git diff --exit-code mise.lock` -> rc 0. The lock carries ~272 lines the locally installed mise adds for every aqua/github tool alongside the openspec row (1.11.0 -> 1.13.1); regenerating demands them, and stripping them would fail this same gate on CI
- [x] 1.2 @integration (agent) `bun install --frozen-lockfile` -> exits 0, so `bun.lock` matches `apps/cli/package.json` at the new pin. Re-run on the committed tree: rc 0
- [x] 1.3 @e2e (agent) `mise run cospec -- doctor` -> **amended, and the amendment is the finding**: doctor does NOT print the wrapped binary's version, so it cannot report `1.13.1` and this row's original wording was unsatisfiable. What it does prove is the negative half — `0 error(s), 1 warning(s), 1 info`, with no `openspec-version` and no `openspec-resolve` finding, so resolution and the accepted range are clean. The positive half is proven instead by `$(mise which openspec) --version` -> `1.13.1` and by `test/contract/version-tripwire.test.ts`, which asserts the live binary equals `PINNED_OPENSPEC_VERSION`. Adding a version line to doctor is recorded as a follow-up, not folded in here. (The one WARNING is `gate-hooks`, a pre-existing worktree/doctor detection mismatch: hk hooks did run on every commit in this worktree)
- [x] 1.4 @integration (agent) `mise run vendor:openspec:check` -> exits 0: `vendor:openspec: bundle and third-party notices up to date.` The regenerated notices moved by exactly one line (`@fission-ai/openspec@1.11.0` -> `1.13.1`); the 71-package list did not move
- [x] 1.5 @unit (agent) grep the repo for `1.11.0` -> confirmed: `grep -rln "1\.11\.0" --exclude-dir=node_modules --exclude-dir=.git .` leaves only test-fixture/unit literals (`init.test.ts`, `doctor.test.ts`, `deltas.test.ts`, `contract/fixtures.ts`, `store.test.ts`, `archive-gotchas.test.ts`, `feedback-format.test.ts`, `scenario-preservation.test.ts`, `added-early-sync.test.ts`, `rules/archive.test.ts`, `delta-bullet-markers.test.ts`, `vanilla-openspec` SKILL.md fixture), behaviour-dating src comments (`deltas.ts`, `rules/specs.ts`, `commands/{config,view,list,validate,show}.ts` — all "re-probed at 1.11.0"/"probed at 1.11.0" narrations of past behaviour, per verification 2.3/11.5), the `harness-workflows` spec's "opsx 1.11.0 workflow parity" requirement title (historical — no delta in this change touches that capability), and this change's own proposal/design/tasks/verification prose narrating the bump. Every apps/docs and docs/ page now reads `1.13.1`; zero live pin reads remain at `1.11.0`

## 2. The contract suite is re-probed honestly against the real 1.13.1 binary [critical]

- [x] 2.1 @integration (agent) `mise run test:contract` after the re-probe -> green: 100 pass / 0 fail across 13 files (`env -u FORCE_COLOR -u NO_COLOR -u COLORTERM -u CLICOLOR mise run test:contract`), each rewritten narrative naming an observed 1.13.1 behaviour
- [ ] 2.2 @manual (human) review the contract diff -> no assertion weakened and no test deleted; every changed expectation is traceable to an observed difference in the new binary
- [x] 2.3 @integration (agent) `test/contract/delta-bullet-markers.test.ts` -> its recorded 1.11.0 narrative flips to the 1.13.1 one, proving the `*`/`+` delta is now portable at the pin: all four `expectPinRefuses` cases now run `expectBothApply` — the binary validates the `*`/`+` delta at exit 0 and archives it, and cospec agrees on both. `archive-gotchas`'s trailing-`###` case flipped the same way (1.13.1's `normalizeRequirementName` strips the closing run, so the MODIFIED merges), with the merge writing the delta's own header verbatim — the living spec keeps `### Requirement: Widget rendering ###` on one requirement, not two

## 3. Apply relays the wrapped advisory fields without changing a verdict

- [x] 3.1 @integration (agent) contract: `cospec apply --json` on a no-delta-spec change -> `apply.warnings` is present, typed and length 1, carrying upstream's `specs/widgets.md is not a capability's spec.md …` string, at exit 0; a correctly `skip_specs`-resolved change emits no contradicting warning at all (upstream's `collectApplyWarnings` short-circuits when the specs artifact is in `skippedArtifacts`), so cospec's precedence still decides. `test/contract/apply-json-fields.test.ts` cases (a) and (c); case (a) runs on a forked schema after Track D's `deltas/unread-file` made that state an ERROR on the main lane, and also pins the main-lane exit 1
- [x] 3.2 @integration (agent) contract: a multi-hop-missing change (a forked `feat` whose `tasks.requires` adds `design`) -> `missingArtifacts` is `['tasks']` while `missingPrerequisites` is `['design','tasks']` — a strict superset, length 2 > 1 — and the exit code is cospec's own gate's `2` with `apply.state == 'blocked'`. `test/contract/apply-json-fields.test.ts` case (b)
- [x] 3.3 @integration (agent) the same run's human transcript -> the relayed warnings are printed, not only present in `--json`: `printWarnings` emits one `Warning: …` line per warning, from the one call site both lanes share; asserted in `test/contract/apply-relay-guard.test.ts` cases (c) and (d), both of which run on `applyLegacy` after Track D's `deltas/unread-file` made the clear-gate warning state a cospec ERROR
- [x] 3.4 @unit (agent) parse a wrapped payload that carries neither advisory field -> both are absent rather than emitted as empty arrays, and the relay does not fail: `apply-relay-guard.test.ts` asserts `'warnings' in out === false` **and** `'missingPrerequisites' in out === false` on a payload with neither, while a payload carrying `warnings: []` keeps the empty array. Both fields are optional on `ApplyInstructionsJson`, so a binary that reported nothing round-trips as "not reported", never as "checked and found nothing"

## 4. No relayed guidance instructs a bare openspec command [critical]

- [x] 4.1 @integration (agent) contract on `--json` via `applyLegacy` -> no relayed `instruction` or `warnings` string contains a backtick-delimited command span beginning with `openspec`: case (b) exits 2 and its remedy reads "Create each with `cospec instructions <artifact> --change legacy-blocked`"; the legacy lane's own prose "delegating to openspec." is deliberately left alone, since it is not a command span
- [x] 4.2 @integration (agent) contract on `--json` for a v1-grandfathered change whose upstream state is `blocked` on `verification` -> the relayed remedy names `cospec`, and the exit code is cospec's own gate's: case (a) carries `apply.state == "blocked"` with both spans rewritten, at exit 0
- [~] 4.3 @integration (agent) contract on a change whose `tasks.md` exists but is empty -> defer: unreachable through `cospec apply`. Probed against 1.13.1: the binary does return `state: "blocked"` with ``Add tasks to tasks.md, or rebuild it: Create it with `openspec instructions tasks …` ``, but cospec's Step 2 fast validation raises a `tasks/has-tasks` ERROR and exits 1 before Step 5 ever calls the binary, and the legacy lane reaches that file through the missing-artifacts branch instead. The rewrite is still pinned over that exact upstream string as a unit case, and row 4.8 covers the reachable warning relays that replaced it
- [x] 4.4 @unit (agent) the rewrite over a warning embedding an absolute `.openspec.yaml` path -> the command span is rewritten and the path is byte-for-byte unchanged: the unit suite pins the exact 1.13.1 no-delta-specs string; both of its spans become `cospec` and the embedded path survives intact, and contract case (d) repeats the check against a path the real binary produced
- [x] 4.5 @unit (agent) the rewrite over a span naming a verb cospec does not wrap -> the span is relayed unchanged: upstream's `openspec new change <name>` hint and an `openspec archive foo` span both round-trip untouched, as does `openspec validateur` (a verb-prefixed non-verb) and an un-backticked "see openspec status for details"
- [x] 4.6 @integration (agent) re-probe the 1.13.1 emitted verb set -> confirms `instructions`, `status`, `validate` is still the complete set the strings emit, rather than assuming the 1.13.0 reading: at tag `@fission-ai/openspec@1.13.1`, `describeArtifactRemedy` (`instructions.ts:387-388`) emits `instructions` + `status` and `collectApplyWarnings` (`:478`, `:496-497`) emits `validate` + `instructions`; the only other bare-openspec string in that file is the `newChangeHint` thrown by `validateChangeExists`, which never enters `instruction`/`warnings` and is unreachable anyway because cospec resolves the change name itself first
- [ ] 4.7 @eval (agent) `mise run eval:e2e` against the relay guard's output -> the advisory DeepSeek harness shows no regression in how an agent reads a blocked `cospec apply`, and no sampled completion follows a bare-`openspec` instruction
- [x] 4.8 @integration (agent) contract on the two reachable warning relays -> `applyLegacy` rewrites 1.13.1's unread-delta-file warning to `cospec validate <name>` at exit 0, and `applyLegacy` on a schema whose `apply.requires` omits `specs` rewrites both spans of the no-delta-specs warning while the absolute `.openspec.yaml` path it embeds arrives byte-identical: contract cases (c) and (d). **Amended by Track D:** case (c) was written on the MAIN lane's clear gate, which `deltas/unread-file` then closed — the same change on a cospec type now exits 1 naming that rule, which case (c) also pins. No relayed warning is reachable on the main lane at 1.13.1: every upstream warning state is a cospec ERROR first

## 5. The archive-preflight INFO family is deduped without losing anything [critical]

- [x] 5.1 @integration (agent) contract: a would-be-refused `MODIFIED` delta -> exactly one finding (cospec's `archive/target-missing`), the upstream INFO twin suppressed, and `valid` plus the exit code unchanged from 1.11.0: `archive-preflight-dedupe.test.ts` — `byRule['archive/target-missing'] === 1`, no message beginning `Archive would refuse this delta:` survives the merge, exactly one issue names the requirement, and `cospec validate --strict` still exits 1 on cospec's own ERROR
- [x] 5.2 @integration (agent) the same run -> at most one archive-preflight INFO per delta file, confirming upstream's path-keyed suppression bound: the raw binary's `--json` for the same change carries exactly one `Archive would refuse this delta:` INFO, at level INFO, for the one delta file
- [x] 5.3 @integration (agent) contract: a `MODIFIED` header mismatch in content, for which cospec has no twin -> the delegated issue survives the merge unchanged: pinned as a unit case in `delegated-dedupe.test.ts` over 1.13.1's exact `- header mismatch in content` throw, kept alongside an `archive/target-missing` finding on the same file. Not reachable as a contract case — the mismatch requires a MODIFIED whose own header line disagrees with its section entry, which the delta reader resolves to one name before the merge sees it
- [x] 5.4 @unit (agent) each new `DUPLICATE_CLASSES` `nativeKey` capture against a message carrying the `, but "…" exists` and `and differs only in case or spacing` tails -> the capture stops before the tail and matches the native key: 11 cases in `delegated-dedupe.test.ts` over the verbatim 1.13.1 throws — REMOVED and RENAMED-source with the `, but "### Requirement: X" exists; fix the header to match it exactly` tail, and both case-collision shapes — plus the negative cases (a different requirement, a different capability file) that prove the key is not matching everything
- [x] 5.5 @integration (agent) contract: a `tasks.md` with only plain list items -> exactly one finding, cospec's `tasks/has-tasks` ERROR, with upstream's zero-task WARNING suppressed: `byRule['tasks/has-tasks'] === 1` and no issue quoting `counts as 0 tasks`. Reaching the delegated warning at all needed the composed `feat` schema copied into the temp store — openspec reads `tracks:` from the schema and tracks nothing without it — and the same run asserts the raw binary really did emit the warning being suppressed
- [x] 5.6 @unit (agent) the narrowed `deltas/spec-at-specs-root` regex against upstream's unread-file message for `specs/spec.md.md` -> does not match, so the unread-file defect is no longer wrongly suppressed: with both findings present on one change, the merge keeps cospec's root-level ERROR and the delegated `specs/spec.md.md` message, in that order. The row also gained a `nativeKey`, so the pairing is keyed on the path both messages name
- [x] 5.7 @integration (agent) `mise run cospec-validate-all` (`ci.yml`) -> `0 errors, 0 warnings — validation passed` over 1 change and 20 specs; the only findings are the pre-existing `openspec/validate` INFO lines about long requirement text, each appearing once — no doubled count, and no new rule fires on this repo's own tree

## 6. cospec's archive gates agree with the 1.13.1 binary in both directions [critical]

- [x] 6.1 @integration (agent) contract: a case-only `ADDED` collision -> `archive/added-exists` at `cospec validate --strict`, the delegated 1.13.1 ERROR deduped, and `cospec archive` refusing at pre-flight rather than at delegation: the real binary aborts with `already exists and differs only in case or spacing`; cospec reports one `archive/added-exists` naming the living spelling, and its archive refuses without the binary's `Aborted. No files were changed.` banner ever appearing — the proof it never delegated
- [x] 6.2 @integration (agent) contract: a case-only `RENAMED`-target collision -> the same, and a case-only rename (`Foo` → `foo`) is **not** reported as a collision: the collision is one `archive/added-exists` on both sides; the case-only rename validates clean and BOTH sides archive it, leaving `### Requirement: WIDGET CACHING` in the living spec. An ADDED folding onto a name the same delta REMOVES is likewise applied by both — re-probed against the binary, because upstream's near-miss search reads the spec after RENAMED and REMOVED have run
- [x] 6.3 @integration (agent) contract: an early-synced `ADDED` whose only fold-equal living name is itself -> no `archive/added-exists`, the archive proceeds: `added-early-sync.test.ts` still passes unchanged — the identical-body ADDED validates clean and archives at exit 0 on both sides. The fold arm cannot reach it: it runs only when the exact name is absent, and unit cases pin both branches (exact collision keeps its own message; an early-synced RENAMED gains no fold finding)
- [x] 6.4 @integration (agent) contract: an unpaired `FROM:` -> refused by `cospec validate --strict` at exit 1, reported exactly once, and the delegated `buildUpdatedSpec` throw is never reached (no `Archive would refuse this delta:` INFO in the report — upstream's own validator stops the change first). `test/contract/parity-close-out.test.ts`. **This row found a live dedupe gap:** 1.13.1 added its own unpaired-FROM/TO ERROR (`validation/validator.ts:236`, absent at 1.13.0 and 1.11.0) whose first sentence is byte-identical to cospec's `deltas/unpaired-rename`, so the defect was being reported twice. A `DUPLICATE_CLASSES` entry keyed on the whole `<side>: "<name>" has no matching <side>: line` span closes it; five unit cases in `delegated-dedupe.test.ts` pin the boundary (other side, other requirement, other file, non-matching shape all stay kept)
- [~] 6.5 @integration (agent) contract: a delta whose only scenario is a bare header -> defer: the row asserts a parity that does not exist and should not: re-probed at 1.13.1, the binary does NOT refuse it. `openspec validate --strict` reports `Change 'hollow-widget' is valid` at exit 0 and `openspec archive` merges at exit 0, leaving the living spec holding a hollow header with its steps silently gone; upstream's `findMissingCurrentScenarios` compares names through `parseScenarioBlocks`, which never applies `hasScenarioBody`, so the bump did not move this. cospec is deliberately ahead — `archive/scenario-preservation` refuses before delegating — and the mirror case (a living spec that already carries a bodyless header, reproduced faithfully) still archives, so the strictness costs no false refusal. Both directions are pinned in `test/contract/scenario-preservation.test.ts`; what is deferred is only the claim of identical refusal
- [x] 6.6 @integration (agent) contract: archive a change whose spec carries a fenced block with two or more consecutive blank lines -> the archived `spec.md` preserves them byte-for-byte through 1.13’s fence-aware collapse. `test/contract/parity-close-out.test.ts`: a MODIFIED carrying ` ```text\nline one\n\n\n\nline two\n``` ` archives at exit 0 and the living spec contains that fence verbatim, with no collapsed variant present
- [x] 6.7 @integration (agent) contract: archive a `retire_capabilities: true` change whose spec has a line-wrapped scenario bullet -> the retirement succeeds. `test/contract/parity-close-out.test.ts`: `cospec archive` exits 0, `openspec/specs/widgets/spec.md` is deleted rather than rebuilt empty, and the change leaves `openspec/changes/`. Reaching the retirement path at all needs the composed `feat` schema in the store — without it 1.13.1 refuses with `The marker present now cannot be honored (schema: unknown schema 'feat')`, which is what the raw probe hit first
- [x] 6.8 @integration (agent) re-probe `archive-parity`, `archive-gotchas`, `scenario-preservation`, `added-early-sync`, `hard-reality` -> each gate refuses exactly what the binary refuses. The full contract suite is green (109 pass / 0 fail across 14 files) after the two disagreements the re-probe exposed were rewritten to 1.13.1 reality rather than relaxed: the trailing-`###` MODIFIED that 1.11.0 aborted on is now applied (matching on the stripped name, writing the raw block), and the four `*`/`+` REMOVED/RENAMED cases the pin used to refuse are now applied by both sides. The one surviving divergence is cospec-stricter and deliberate (row 6.5)

- [x] 6.9 @integration (agent) contract: a delta whose own two operations collide only by case -> refused by cospec's pre-flight, not by the delegated merge. Found in review: the fold arms searched `living.requirementNames` only, so every collision a delta makes with ITSELF was invisible — the real 1.13.1 binary aborts on two `## ADDED` names that fold onto each other (`ADDED failed for header … already exists and differs only in case or spacing`, exit 1, `Aborted. No files were changed.`) while `cospec validate --strict` reported `valid: true`, `0 errors`. Fixed by replaying the delta the way upstream applies it (`replayDeltaNames`, `core/rules/archive.ts`: RENAMED, then REMOVED, then MODIFIED, then ADDED) and folding each op against the spec as it stands when that op runs. Observed in `archive-preflight-dedupe.test.ts`: the binary refuses the two-ADDED delta; cospec reports exactly one `archive/added-exists` naming the earlier ADDED and saying it came from an earlier operation in this delta, no `Archive would refuse this delta:` INFO survives the merge, and `cospec archive` refuses without the binary's abort banner. An `## ADDED` folding onto the delta's own `## RENAMED` target is refused by both the same way; five unit cases in `rules/archive.test.ts` (including a second `## RENAMED` target folding onto the first, and the new-capability shape with no living spec) all fail against the pre-fix rule and pass after
- [x] 6.10 @integration (agent) contract: a delta that reuses a name an earlier operation vacated -> applied by both, no false refusal and no false invariant breach. Observed: a swap (`Widget rendering` -> `Widget streaming`, then `Widget caching` -> `Widget rendering`) archives at exit 0 on the raw binary and on cospec, leaving both requirement headers in the living spec. Pre-fix cospec refused it at validate with `archive/added-exists` against the pristine living name. Fixing that exposed a second stale-view defect on the same delta shape, in `cospec archive`'s post-merge spot-check: judging each op alone against the end state, it reported `RENAMED from 'Widget rendering' still present in living spec — this is a cospec/openspec invariant breach; please file a bug` after the merge had succeeded. `spotCheckMiss` now judges each op against the capability's net effect (`netEffect`, `commands/archive.ts`), so a name another operation legitimately puts back is not a miss

## 7. A delta-shaped file at an unread path is no longer invisible

- [x] 7.1 @integration (agent) contract: a change carrying `specs/user-auth.md` with a delta section header -> `deltas/unread-file` ERROR, the delegated twin deduped, and `cospec archive` no longer treating it as a clean no-op: beside a real delta the file yields exactly one finding (`deltas/unread-file`, ERROR) where the raw binary reports `Delta spec found at specs/widgets/notes.md`; as a change's ONLY spec content (`specs/widgets.md`) `cospec archive` exits non-zero and leaves the change in place, where the zero-delta leniency used to archive it with nothing merged
- [x] 7.2 @integration (agent) contract: a change carrying `specs/user-auth/delta.md` -> `deltas/unread-file` ERROR naming that path: the message names `specs/widgets/delta.md` and the hint names `specs/widgets/spec.md`; archive refuses. The `expected` path is derived the way openspec derives it — from the capability folder, or from the file's own name when it sits at the specs/ root
- [x] 7.3 @unit (agent) a companion note with no delta section header beside a real `spec.md` -> no `deltas/unread-file` issue: unit case in `rules/deltas.test.ts` (a note quoting a bare `### Requirement:` block is silent), and the contract pair confirms both readers agree — the raw binary's report never mentions the file either. `scenario-preservation.test.ts`'s companion-note case was rewritten to the same shape, since its old fixture quoted a `## MODIFIED Requirements` header and is now (correctly) refused by both sides

## 8. The nested-change deferral is bounded, not assumed [critical]

- [x] 8.1 @integration (agent) contract: a hand-created namespace folder under `openspec/changes/` -> **the row's prediction is falsified and the honest result is recorded instead.** The raw 1.13.1 binary does emit the ERROR verbatim (`"ns-wrap" is not a change: it is a folder wrapping openspec/changes/ns-wrap/real-change/. …`) at exit 1, and `test/contract/parity-close-out.test.ts` pins that string. But it does NOT reach the user through cospec: the wrapper folder has no `.openspec.yaml`, so cospec's Step 2 fast validation raises `meta/openspec-yaml` and exits 1 before ever delegating. `cospec validate` therefore refuses — the safety property holds — while telling the reader the file is missing rather than that the folder is a namespace, with a hint (`cospec new <type> <slug>`) that misdirects. The test asserts both halves, including the absence of `folder wrapping` from cospec's report, so the deferred nested-change work has a failing-by-design marker to flip
- [x] 8.2 @integration (agent) contract: `cospec archive <namespace-folder>` -> refused at exit 1 with nothing moved: the change's files stay in place under `openspec/changes/ns-wrap/real-change/` and no dated directory appears under `openspec/changes/archive/`. The raw binary's hard refusal (`archive_change_is_namespace_folder`, surfaced as `Cannot archive 'ns-wrap': "ns-wrap" is not a change: it is a folder wrapping …`, before validation) is pinned separately in the same file. As at 8.1, cospec's refusal is its own `meta/openspec-yaml` ERROR, not the relayed message — the deferral is bounded on safety, not on message quality

## 9. The composed schemas survive 1.13.1's load-time validation [critical]

- [x] 9.1 @integration (agent) `openspec schema validate` against each of the 11 composed schemas -> all 11 valid under the raw 1.13.1 binary (`build chore ci docs feat fix perf refactor revert style test`), each `✓ Schema '<name>' is valid` at rc 0; zero `apply.requires` load errors and zero `apply.tracks` mismatch warnings
- [x] 9.2 @integration (agent) `mise run openspec:schema:validate` (`ci.yml`) -> green, all 11 schemas valid under the raw binary, so no cospec command breaks on schema load at the new pin

## 10. Inherited upstream fixes actually reach cospec's surfaces

- [x] 10.1 @integration (agent) contract: `cospec store setup --no-init-git` inside an existing git repo -> succeeds at exit 0 with `"git": {"is_repository": false, "initialized": false}`, where the 1.11.0 pin falsely refused. Pinned as a new case in `test/contract/store.test.ts` (3 -> 4 tests), hand-probed against the raw binary first
- [x] 10.2 @integration (agent) contract: `cospec config edit` with a multi-word `EDITOR` -> spawns the editor, where 1.11.0 falsely failed. Probed with `EDITOR="<fake> --wait"`: exit 0, and the fake editor logs `--wait <xdg>/openspec/config.json`, proving the argv split. Pinned as a new case in `test/contract/config-surface.test.ts`
- [x] 10.3 @integration (agent) re-probe `store.test.ts` against a wedged git tree -> no hang: the whole file runs green against the real 1.13.1 binary inside the suite timeout, confirming upstream’s git-probe timeout and `maxBuffer` fix reaches cospec’s store surface. `bun test test/contract/store.test.ts test/contract/config-surface.test.ts` -> 20 pass / 0 fail
- [x] 10.4 @integration (agent) re-probe `config-surface.test.ts` -> green with no assertion changed: no config-schema key moved, and `defaultStore` still resolves only as a fallback after local-root resolution fails. Its stale re-probe stamp was restamped to the 1.13.1 pin

## 11. Native surfaces the upstream changes brush against still hold

- [x] 11.1 @integration (agent) `DELEGATED_DELTA_PATH_RE` against a nested capability -> still maps `<specId>/spec.md`, so the dedupe pairings fire for nested capabilities too: verified through the behaviour rather than the regex — a missing MODIFIED target under `specs/platform/session-layout/spec.md` yields one `archive/target-missing` at that path with no surviving preflight INFO, which only holds if the delegated `platform/session-layout/spec.md` was mapped to cospec's path first
- [x] 11.2 @integration (agent) generate a leftover skill set with the real 1.13.1 binary, then run `cospec doctor` -> detection still holds. Probed end to end: `cospec init --yes` into a fresh repo, then `openspec init --tools agents` (the real 1.13.1 binary) wrote six `.agents/skills/openspec-*/SKILL.md` files, each stamped `metadata.author: openspec` with a bare-semver `generatedBy: "1.13.1"`. `cospec doctor` reported exactly 6 `opsx-leftover` WARNINGs, one per file. The provenance-only predicate (`doctor.ts` `checkOpsx`) is unaffected by upstream's changed legacy cleanup and skill descriptions
- [x] 11.3 @unit (agent) the completions golden/spec test -> unchanged and green (16 pass / 0 fail): cospec still ships no `--report` registry entry and mutates no rc file
- [~] 11.4 @runtime (agent) `cospec feedback` live output -> defer: not safely probeable — `cospec feedback` has no dry-run flag and SUBMITS A REAL GITHUB ISSUE, so it is not a safe probe (an earlier probe in this change filed and had to close aligned-team/cospec#41 and #42). The provenance string it would print is read from the same resolved version every other surface reads, which `$(mise which openspec) --version` -> `1.13.1` and the `version-tripwire` contract test already prove; the formatter itself is unit-tested over its inputs in `feedback-format.test.ts`, whose `1.11.0` literals are deliberately arbitrary fixtures and were left untouched (verification 1.5)
- [x] 11.5 @unit (agent) re-read `core/rules/specs.ts`'s placeholder comment -> the behaviour it dates still holds at 1.13.1: cross-checked against the upstream 1.13.1 clone, `src/core/validation/constants.ts:20` still defines `PURPOSE_PLACEHOLDER_PREFIX = 'TBD - created by archiving change '`, unchanged since the comment was written, so the `1.11.0` in it stays as a behaviour-dating literal, not a stale pin read

## 12. Zero drift between the repo, its managed files, and its docs [critical]

- [x] 12.1 @integration (agent) `mise run generate:check` -> clean; this change touches no canon file, so it must be green from the first commit. Observed: `mise run generate:check` -> `cospec update --check: no drift`
- [x] 12.2 @integration (agent) `mise run agents:check` after editing `.agents/shared.md` and running `mise run agents:sync` -> clean, with the four generated line pairs in `CLAUDE.md`/`AGENTS.md` moved by the sync and not by hand. Observed: edited `.agents/shared.md` lines 11/18/33/164 (`1.11.0` -> `1.13.1`), ran `mise run agents:sync` -> `Synced: CLAUDE.md` / `Synced: AGENTS.md`, then `mise run agents:check` -> `OK: CLAUDE.md`, `OK: AGENTS.md`, `All shared blocks are in sync.` — `CLAUDE.md`/`AGENTS.md` line pairs 15/22/37/168 moved by the sync, never hand-edited
- [x] 12.3 @integration (agent) `mise run docs:build` -> succeeds. Observed: `build complete in 0.89s.`, no errors (the one pre-existing `No matching file found for sidebar link: /` warning is unrelated to this change)
- [x] 12.4 @manual (human) read every page named in the proposal's docs impact -> the pin fact appears on its single owning page, `apps/docs/reference/validation-rules.md` lists `deltas/unread-file` and the INFO class, `apps/docs/concepts/apply-and-archive.md` shows `warnings` and `missingPrerequisites` as advisory, and `apps/cli/THIRD-PARTY-LICENSES.md` reflects the regenerated package list. Observed: read every page in the Track F table — `how-it-relates-to-openspec.md` states `1.13.1` and lists 1.12/1.13/1.13.1 deltas including the four 1.13.1 refusals; `apply-and-archive.md`'s JSON example carries `warnings`/`missingPrerequisites` with an advisory-never-blocking note; `validation-rules.md` carries `deltas/unread-file`, the widened `archive/added-exists` case-fold description, and a new "The `openspec/validate` INFO class and dedupe" section naming all pairings; `docs/validation.md`, `docs/architecture.md` (relay-guard + nested-change-deferral rationale + one-parser note), `docs/harness-integration.md`, `apps/docs/guide/harness-setup.md`, `docs/stores.md`, `docs/self-hosting.md`, `docs/bench.md`, `README.md`, `apps/cli/README.md` all read `1.13.1`; `apps/cli/THIRD-PARTY-LICENSES.md:14` already reads `@fission-ai/openspec@1.13.1` (Track A)
- [x] 12.5 @integration (agent) `mise run check` -> green end to end, exit 0. Final run over the finished tree, re-run after the review fixes in rows 6.9/6.10: lint, typecheck, format, generate:check, agents:check, docs:build, `cospec validate --all --strict`, `openspec schema validate` (all 11), unit 958 pass / 0 fail, bench 339 pass / 0 fail, integration 163 pass / 0 fail, contract 113 pass / 0 fail, release/pack 14 pass / 0 fail

## 13. Every ledger decision was actually reviewed

- [ ] 13.1 @manual (human) walk the parity plan's decision ledger -> each item was reviewed at implementation time and its recorded disposition (PORT, INHERIT, COVERED, DEFER, N/A) still matches what the code does after the bump; any item whose disposition changed is named here with the new one
- [x] 13.2 @integration (agent) record every disposition the implementation changed, so 13.1's human walk starts from the deltas rather than the whole ledger -> seven moved. (1) The nested-change deferral's bound is NOT the delegated message: rows 8.1/8.2 show cospec refusing on its own `meta/openspec-yaml` ERROR and never delegating, so the deferred work now has a UX gap to close, not just a reporting gap. (2) The unpaired `FROM:`/`TO:` item moves from "cospec LEADS the pin" to "the pin caught up": 1.13.1 added its own ERROR with a byte-identical first sentence, which needed a fourth new `DUPLICATE_CLASSES` entry the plan did not name (row 6.4). (3) The bodyless-scenario item moves from PARITY to COSPEC-STRICTER: re-probed at 1.13.1 the binary still validates and archives a hollowed MODIFIED at exit 0 (row 6.5). (4) The empty-`tasks.md` relay moves from PORT to UNREACHABLE — Step 2's `tasks/has-tasks` ERROR exits before Step 5 delegates (row 4.3). (5) The archive fold arms needed a third exclusion the plan did not name — living names the same delta vacates via `REMOVED`/`RENAMED`-source — without which cospec would refuse an archive 1.13.1 performs (row 6.2). (6) `cospec doctor` cannot satisfy row 1.3 as written: it never prints the wrapped binary's version (row 1.3). (7) `cospec feedback` is not a usable probe: it submits a real GitHub issue with no dry-run (row 11.4). Every other ledger item's recorded disposition still matches the code
