# Dependencies

## Blocked by

- [x] `store-aware-commands` — provides the `Root`/`resolveRoot`/`--store`
      _(archived 2026-07-09)_ threading (`apps/cli/src/core/root.ts`,
      `root.storeArgs`) that every new passthrough and the
      `store`/`context`/`workset` groups in this change build on. Implementation
      and tests are complete (all `tasks.md` rows checked) but the change is
      still unarchived in `openspec/changes/` on `main` as of this writing — box
      stays unchecked until it is archived.

## Soft-blocked by

None.
