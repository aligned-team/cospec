# Changelog

## Unreleased

- Changed `greet()` to uppercase the name and end with `!!!` — this reads as
  shouting and users have complained. Needs reverting.

## v1.0.0

- Initial `greet()`: returns `Hello, ${name}!` (e.g. `greet('Ada')` returns
  `'Hello, Ada!'`).
