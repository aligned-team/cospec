# orders-hard-sample

A small multi-file order-processing library: an inventory ledger
(`src/inventory.ts`), a cart (`src/cart.ts`), pricing/discount/tax rules
(`src/pricing.ts`), and a checkout flow that ties them together
(`src/orders.ts`). There is no store-credit support yet.
