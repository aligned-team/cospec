# oxfmt/prettier ignore.
#
# cospec-generated, generator-owned files. These are composed from
# apps/cli/src/canon/** by the composer/renderer and are byte-exact drift-gated
# by `mise run generate:check` (= `cospec update --check`). The generator is
# their sole formatting authority: running the hand-formatter over them would
# make `format:fix` and `generate:check` mutually unsatisfiable. They remain
# git-tracked (committed, reviewable) — only formatting is delegated to cospec.
# Generated vendored openspec bundle embedded in the compiled binary. Built by
# `mise run vendor:openspec` (drift-gated by `vendor:openspec:check`); it is
# minified third-party output, not hand-formatted source.
apps/cli/src/vendor/
# Generated third-party license notices for the vendored openspec bundle. Built
# alongside the bundle by `mise run vendor:openspec` (drift-gated); it reproduces
# upstream license text verbatim and must not be reflowed by the formatter.
apps/cli/THIRD-PARTY-LICENSES.md

openspec/schemas/
.claude/commands/cospec/
.claude/skills/cospec-*/
# The shared vendor-neutral skills root, written by the `codex` and `agents`
# targets (it replaced `.codex/skills/`).
.agents/skills/cospec-*/
.codex/rules/cospec.rules
.opencode/commands/cospec-*.md
.opencode/skills/cospec-*/

# packages/bench `style` scenario fixture: deliberately seeded violating this
# repo's own oxfmt conventions (double quotes, semicolons) — that IS the bug
# the scenario's agent is scored on fixing inside its sandbox copy. The
# checked-in fixture itself must never be reformatted, or the scenario would
# start pre-completed.
packages/bench/scenarios/fixtures/style/src/format.ts
